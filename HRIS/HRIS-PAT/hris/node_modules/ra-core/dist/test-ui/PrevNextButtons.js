import * as React from 'react';
import { LinkBase } from "../routing/index.js";
import { useTranslate } from "../i18n/useTranslate.js";
import { usePrevNextController, } from "../controller/usePrevNextController.js";
export const PrevNextButtons = (props) => {
    const { hasPrev, hasNext, prevPath, nextPath, index, total, error, isPending, } = usePrevNextController(props);
    const translate = useTranslate();
    if (isPending) {
        return React.createElement("p", null, "Loading...");
    }
    if (error) {
        return React.createElement("p", null, error.message);
    }
    if (!hasPrev && !hasNext) {
        return null;
    }
    return (React.createElement("div", { style: { display: 'flex', alignItems: 'center', gap: 8 } },
        hasPrev && prevPath ? (React.createElement(LinkBase, { to: prevPath }, translate('ra.navigation.previous'))) : (React.createElement("span", { style: { opacity: 0.5 } }, translate('ra.navigation.previous'))),
        typeof index === 'number' && (React.createElement("span", null,
            index + 1,
            " / ",
            total)),
        hasNext && nextPath ? (React.createElement(LinkBase, { to: nextPath }, translate('ra.navigation.next'))) : (React.createElement("span", { style: { opacity: 0.5 } }, translate('ra.navigation.next')))));
};
//# sourceMappingURL=PrevNextButtons.js.map