import * as React from 'react';
import { LinkBase } from "../routing/index.js";
import { useRecordContext } from "../controller/record/useRecordContext.js";
import { useResourceContext } from "../core/useResourceContext.js";
export const ShowButton = (props) => {
    const record = useRecordContext(props);
    const resource = useResourceContext(props);
    if (!record)
        return null;
    return (React.createElement(LinkBase, { to: `/${resource}/${record.id}/show`, onClick: e => {
            e.stopPropagation();
        } }, "Show"));
};
//# sourceMappingURL=ShowButton.js.map