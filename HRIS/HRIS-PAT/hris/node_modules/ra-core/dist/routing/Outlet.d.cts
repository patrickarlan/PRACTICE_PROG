import * as React from 'react';
import type { RouterOutletProps } from "./RouterProvider.cjs";
export type { RouterOutletProps as OutletProps } from "./RouterProvider.cjs";
/**
 * Outlet component for nested route rendering.
 * This is a router-agnostic wrapper that uses the configured router provider.
 *
 * @example
 * import { Outlet } from 'react-admin';
 *
 * const MyLayout = () => (
 *     <div>
 *         <header>My App</header>
 *         <main>
 *             <Outlet />
 *         </main>
 *     </div>
 * );
 */
export declare const Outlet: (props: RouterOutletProps) => React.JSX.Element;
//# sourceMappingURL=Outlet.d.ts.map