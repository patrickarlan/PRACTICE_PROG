import * as React from 'react';
import type { RouterNavigateProps } from "./RouterProvider.js";
export type { RouterNavigateProps as NavigateProps } from "./RouterProvider.js";
/**
 * Navigate component for declarative navigation.
 * This is a router-agnostic wrapper that uses the configured router provider.
 *
 * @example
 * import { Navigate } from 'react-admin';
 *
 * const MyComponent = () => (
 *     <Navigate to="/login" replace />
 * );
 */
export declare const Navigate: (props: RouterNavigateProps) => React.JSX.Element;
//# sourceMappingURL=Navigate.d.ts.map