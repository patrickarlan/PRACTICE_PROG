import { useIsOffline } from "./useIsOffline.js";
export const IsOffline = ({ children }) => {
    const isOffline = useIsOffline();
    return isOffline ? children : null;
};
//# sourceMappingURL=IsOffline.js.map