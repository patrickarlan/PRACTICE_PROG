import { UseUpdateOptions } from "../../dataProvider/useUpdate.js";
import { RedirectionSideEffect } from "../../routing/useRedirect.js";
import { RaRecord, MutationMode } from "../../types.js";
/**
 * Prepare a set of callbacks for an update button
 *
 * @example
 * const UpdateButton = ({
 *     resource,
 *     record,
 *     redirect,
 *     ...rest
 * }) => {
 *     const {
 *         isPending,
 *         handleUpdate,
 *     } = useUpdateController({
 *         mutationMode: 'pessimistic',
 *         resource,
 *         record,
 *         redirect,
 *     });
 *
 *     const [open, setOpen] = useState(false);
 *
 *     return (
 *         <Fragment>
 *             <Button
 *                 onClick={() => setOpen(true)}
 *                 label="ra.action.update"
 *                 {...rest}
 *             >
 *                 {icon}
 *             </Button>
 *             <Confirm
 *                 isOpen={open}
 *                 loading={isPending}
 *                 title="ra.message.update_title"
 *                 content="ra.message.update_content"
 *                 titleTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 contentTranslateOptions={{
 *                     name: resource,
 *                     id: record.id,
 *                 }}
 *                 onConfirm={() => handleUpdate()}
 *                 onClose={() => setOpen(false)}
 *             />
 *         </Fragment>
 *     );
 * };
 */
export declare const useUpdateController: <RecordType extends RaRecord = any, ErrorType = Error>(props: UseUpdateControllerParams<RecordType, ErrorType>) => UseUpdateControllerReturn<RecordType>;
export interface UseUpdateControllerParams<RecordType extends RaRecord = any, MutationOptionsError = unknown> {
    mutationMode?: MutationMode;
    mutationOptions?: UseUpdateOptions<RecordType, MutationOptionsError>;
    record?: RecordType;
    redirect?: RedirectionSideEffect;
    resource?: string;
    successMessage?: string;
}
export interface UseUpdateControllerReturn<RecordType extends RaRecord = any> {
    isLoading: boolean;
    isPending: boolean;
    handleUpdate: (data: Partial<RecordType>) => void;
}
//# sourceMappingURL=useUpdateController.d.ts.map