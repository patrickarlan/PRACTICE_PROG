import * as React from 'react';
import { ReactNode } from 'react';
import { RaRecord } from "../../types.js";
import { EditControllerProps, EditControllerResult } from "./useEditController.js";
/**
 * Call useEditController and put the value in a EditContext
 *
 * Base class for <Edit> components, without UI.
 *
 * Accepts any props accepted by useEditController:
 * - id: The record identifier
 * - resource: The resource
 *
 * @example // Custom edit layout
 *
 * const PostEdit = () => (
 *     <EditBase resource="posts">
 *         <Grid container>
 *             <Grid item xs={8}>
 *                 <SimpleForm>
 *                     ...
 *                 </SimpleForm>
 *             </Grid>
 *             <Grid item xs={4}>
 *                 Edit instructions...
 *             </Grid>
 *         </Grid>
 *         <div>
 *             Post related links...
 *         </div>
 *     </EditBase>
 * );
 */
export declare const EditBase: <RecordType extends RaRecord = any, ErrorType = Error>({ authLoading, loading, offline, error, redirectOnError, children, render, ...props }: EditBaseProps<RecordType, ErrorType>) => React.JSX.Element;
export interface EditBaseProps<RecordType extends RaRecord = RaRecord, ErrorType = Error> extends EditControllerProps<RecordType, ErrorType> {
    authLoading?: ReactNode;
    loading?: ReactNode;
    offline?: ReactNode;
    error?: ReactNode;
    children?: ReactNode;
    render?: (props: EditControllerResult<RecordType, ErrorType>) => ReactNode;
}
//# sourceMappingURL=EditBase.d.ts.map