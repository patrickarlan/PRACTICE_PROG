"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReferenceOneFieldBase = void 0;
const react_1 = __importStar(require("react"));
const useReferenceOneFieldController_1 = require("./useReferenceOneFieldController.cjs");
const record_1 = require("../record/index.cjs");
const core_1 = require("../../core/index.cjs");
const ReferenceFieldContext_1 = require("./ReferenceFieldContext.cjs");
const routing_1 = require("../../routing/index.cjs");
/**
 * Render the related record in a one-to-one relationship
 *
 * Expects a single field as child
 *
 * @example // display the bio of the current author
 * <ReferenceOneFieldBase reference="bios" target="author_id">
 *     <TextField source="body" />
 * </ReferenceOneFieldBase>
 */
const ReferenceOneFieldBase = (props) => {
    const { children, render, record, reference, source = 'id', target, empty, error, loading, sort, filter, link, offline, queryOptions, } = props;
    const controllerProps = (0, useReferenceOneFieldController_1.useReferenceOneFieldController)({
        record,
        reference,
        source,
        target,
        sort,
        filter,
        queryOptions,
    });
    const path = (0, routing_1.useGetPathForRecord)({
        record: controllerProps.referenceRecord,
        resource: reference,
        link,
    });
    const context = (0, react_1.useMemo)(() => ({
        ...controllerProps,
        link: path,
    }), [controllerProps, path]);
    if (!render && !children) {
        throw new Error("<ReferenceOneFieldBase> requires either a 'render' prop or 'children' prop");
    }
    const recordFromContext = (0, record_1.useRecordContext)(props);
    const { error: controllerError, isPending, isPaused, referenceRecord, } = controllerProps;
    const shouldRenderLoading = !isPaused && isPending && loading !== false && loading !== undefined;
    const shouldRenderOffline = isPaused && isPending && offline !== false && offline !== undefined;
    const shouldRenderError = !!controllerError && error !== false && error !== undefined;
    const shouldRenderEmpty = (!recordFromContext ||
        (!isPaused &&
            referenceRecord == null &&
            !controllerError &&
            !isPending)) &&
        empty !== false &&
        empty !== undefined;
    return (react_1.default.createElement(core_1.ResourceContextProvider, { value: reference },
        react_1.default.createElement(ReferenceFieldContext_1.ReferenceFieldContextProvider, { value: context },
            react_1.default.createElement(record_1.RecordContextProvider, { value: referenceRecord }, shouldRenderLoading
                ? loading
                : shouldRenderOffline
                    ? offline
                    : shouldRenderError
                        ? error
                        : shouldRenderEmpty
                            ? empty
                            : render
                                ? render(controllerProps)
                                : children))));
};
exports.ReferenceOneFieldBase = ReferenceOneFieldBase;
//# sourceMappingURL=ReferenceOneFieldBase.js.map