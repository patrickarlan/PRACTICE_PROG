import React from 'react';
import { useTranslate } from "./useTranslate.js";
export const Translate = ({ i18nKey, options, children }) => {
    const translate = useTranslate();
    const translatedMessage = translate(i18nKey, typeof children === 'string' ? { _: children, ...options } : options);
    if (translatedMessage) {
        return React.createElement(React.Fragment, null, translatedMessage);
    }
    return children;
};
//# sourceMappingURL=Translate.js.map