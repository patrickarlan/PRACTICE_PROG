"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useLoading = void 0;
const react_1 = __importDefault(require("react"));
const react_query_1 = require("@tanstack/react-query");
/**
 * Get the state of the dataProvider connection.
 *
 * Returns true if a query or a mutation is pending.
 *
 * Custom implementation because react-query's useIsFetching and useIsMutating
 * render each time the number of active queries changes, which is too often.
 *
 * @see useIsFetching
 * @see useIsMutating
 */
const useLoading = () => {
    const client = (0, react_query_1.useQueryClient)();
    const mountedRef = react_1.default.useRef(false);
    const isFetchingRef = react_1.default.useRef(client.isFetching() > 0);
    const isMutatingRef = react_1.default.useRef(client.isMutating() > 0);
    const [isLoading, setIsLoading] = react_1.default.useState(isFetchingRef.current || isMutatingRef.current);
    react_1.default.useEffect(() => {
        mountedRef.current = true;
        const unsubscribeQueryCache = client.getQueryCache().subscribe(react_query_1.notifyManager.batchCalls(() => {
            if (mountedRef.current) {
                isFetchingRef.current = client.isFetching() > 0;
                setIsLoading(isFetchingRef.current || isMutatingRef.current);
            }
        }));
        const unsubscribeMutationCache = client.getMutationCache().subscribe(react_query_1.notifyManager.batchCalls(() => {
            if (mountedRef.current) {
                isMutatingRef.current = client.isMutating() > 0;
                setIsLoading(isFetchingRef.current || isMutatingRef.current);
            }
        }));
        return () => {
            mountedRef.current = false;
            unsubscribeQueryCache();
            unsubscribeMutationCache();
        };
    }, [client]);
    return isLoading;
};
exports.useLoading = useLoading;
//# sourceMappingURL=useLoading.js.map