import * as React from 'react';
import FormControlLabel from '@mui/material/FormControlLabel';
import Radio from '@mui/material/Radio';
import { useChoices } from 'ra-core';
import { styled, useThemeProps, } from '@mui/material/styles';
import { sanitizeInputRestProps } from "./sanitizeInputRestProps.js";
export const RadioButtonGroupInputItem = (props) => {
    const { choice, optionText, optionValue, source, translateChoice, disableValue = 'disabled', ...rest } = useThemeProps({
        props: props,
        name: PREFIX,
    });
    const { getChoiceText, getChoiceValue, getDisableValue } = useChoices({
        optionText,
        optionValue,
        translateChoice,
        disableValue,
    });
    const label = getChoiceText(choice);
    const value = getChoiceValue(choice);
    const disabled = getDisableValue(choice);
    const nodeId = `${source}_${value}`;
    return (React.createElement(StyledFormControlLabel, { label: label, htmlFor: nodeId, value: value, disabled: disabled, control: React.createElement(Radio, { id: nodeId, color: "primary" }), ...sanitizeInputRestProps(rest) }));
};
export default RadioButtonGroupInputItem;
const PREFIX = 'RaRadioButtonGroupInputItem';
const StyledFormControlLabel = styled(FormControlLabel, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=RadioButtonGroupInputItem.js.map