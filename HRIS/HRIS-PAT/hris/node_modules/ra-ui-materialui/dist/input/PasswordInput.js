import * as React from 'react';
import { useState } from 'react';
import { useTranslate } from 'ra-core';
import { InputAdornment, IconButton } from '@mui/material';
import Visibility from '@mui/icons-material/Visibility';
import VisibilityOff from '@mui/icons-material/VisibilityOff';
import { styled, useThemeProps, } from '@mui/material/styles';
import { TextInput } from "./TextInput.js";
export const PasswordInput = (props) => {
    const { initiallyVisible = false, ...rest } = useThemeProps({
        props: props,
        name: PREFIX,
    });
    const [visible, setVisible] = useState(initiallyVisible);
    const translate = useTranslate();
    const handleClick = () => {
        setVisible(!visible);
    };
    return (React.createElement(StyledTextInput, { type: visible ? 'text' : 'password', size: "small", InputProps: {
            endAdornment: (React.createElement(InputAdornment, { position: "end" },
                React.createElement(IconButton, { "aria-label": translate(visible
                        ? 'ra.input.password.toggle_visible'
                        : 'ra.input.password.toggle_hidden'), onClick: handleClick, size: "small" }, visible ? (React.createElement(Visibility, { fontSize: "small" })) : (React.createElement(VisibilityOff, { fontSize: "small" }))))),
        }, ...rest }));
};
const PREFIX = 'RaPasswordInput';
const StyledTextInput = styled(TextInput, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=PasswordInput.js.map