"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NumberInput = void 0;
const React = __importStar(require("react"));
const clsx_1 = __importDefault(require("clsx"));
const TextField_1 = __importDefault(require("@mui/material/TextField"));
const ra_core_1 = require("ra-core");
const styles_1 = require("@mui/material/styles");
const InputHelperText_1 = require("./InputHelperText.cjs");
const sanitizeInputRestProps_1 = require("./sanitizeInputRestProps.cjs");
/**
 * An Input component for a number
 *
 * @example
 * <NumberInput source="nb_views" />
 *
 * You can customize the `step` props (which defaults to "any")
 * @example
 * <NumberInput source="nb_views" step={1} />
 *
 */
const NumberInput = (props) => {
    const { className, defaultValue = null, format = convertNumberToString, helperText, label, margin, onChange, onBlur, onFocus, parse, resource, source, step = 'any', min, max, validate, variant, inputProps: overrideInputProps, disabled, readOnly, ...rest } = (0, styles_1.useThemeProps)({
        props: props,
        name: PREFIX,
    });
    const { field, fieldState: { error, invalid }, id, isRequired, } = (0, ra_core_1.useInput)({
        defaultValue,
        onBlur,
        resource,
        source,
        validate,
        disabled,
        readOnly,
        ...rest,
    });
    const { onBlur: onBlurFromField } = field;
    const inputProps = { ...overrideInputProps, step, min, max };
    // This is a controlled input that renders directly the string typed by the user.
    // This string is converted to a number on change, and stored in the form state,
    // but that number is not not displayed.
    // This is to allow transitory values like '1.0' that will lead to '1.02'
    // text typed by the user and displayed in the input, unparsed
    const [value, setValue] = React.useState(format(field.value));
    const hasFocus = React.useRef(false);
    // update the input text when the record changes
    React.useEffect(() => {
        if (!hasFocus.current) {
            const stringValue = format(field.value);
            setValue(value => (value !== stringValue ? stringValue : value));
        }
    }, [field.value, format]);
    // update the input text when the user types in the input
    const handleChange = (event) => {
        if (onChange) {
            onChange(event);
        }
        if (typeof event.target === 'undefined' ||
            typeof event.target.value === 'undefined') {
            return;
        }
        const target = event.target;
        setValue(target.value);
        const newValue = target.valueAsNumber !== undefined &&
            target.valueAsNumber !== null &&
            !isNaN(target.valueAsNumber)
            ? parse
                ? parse(target.valueAsNumber)
                : target.valueAsNumber
            : parse
                ? parse(target.value)
                : convertStringToNumber(target.value);
        field.onChange(newValue);
    };
    const handleFocus = (event) => {
        if (onFocus) {
            onFocus(event);
        }
        hasFocus.current = true;
    };
    const handleBlur = (event) => {
        if (onBlurFromField) {
            onBlurFromField(event);
        }
        hasFocus.current = false;
        const stringValue = format(field.value);
        setValue(value => (value !== stringValue ? stringValue : value));
    };
    const renderHelperText = helperText !== false || invalid;
    const { ref, ...fieldWithoutRef } = field;
    return (React.createElement(StyledTextField, { id: id, ...fieldWithoutRef, inputRef: ref, 
        // use the locally controlled state instead of the react-hook-form field state
        value: value, onChange: handleChange, onFocus: handleFocus, onBlur: handleBlur, className: (0, clsx_1.default)('ra-input', `ra-input-${source}`, className), type: "number", size: "small", variant: variant, error: invalid, disabled: disabled || readOnly, readOnly: readOnly, helperText: renderHelperText ? (React.createElement(InputHelperText_1.InputHelperText, { error: error?.message, helperText: helperText })) : null, label: label !== '' && label !== false ? (React.createElement(ra_core_1.FieldTitle, { label: label, source: source, resource: resource, isRequired: isRequired })) : null, margin: margin, inputProps: { ...inputProps, readOnly }, ...(0, sanitizeInputRestProps_1.sanitizeInputRestProps)(rest) }));
};
exports.NumberInput = NumberInput;
const convertStringToNumber = value => {
    if (value == null || value === '') {
        return null;
    }
    const float = parseFloat(value);
    return isNaN(float) ? 0 : float;
};
const convertNumberToString = value => value == null || isNaN(value) ? '' : value.toString();
const PREFIX = 'RaNumberInput';
const StyledTextField = (0, styles_1.styled)(TextField_1.default, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=NumberInput.js.map