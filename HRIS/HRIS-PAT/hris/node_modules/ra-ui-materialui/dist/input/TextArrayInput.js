import * as React from 'react';
import clsx from 'clsx';
import { Chip, Autocomplete, TextField, } from '@mui/material';
import { styled, useThemeProps, } from '@mui/material/styles';
import { useInput, FieldTitle } from 'ra-core';
import { InputHelperText } from "./InputHelperText.js";
export const TextArrayInput = (inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { className, disabled, format, helperText, label, margin, parse, readOnly, size, source, sx, validate, variant, ...rest } = props;
    const { field, fieldState: { error, invalid }, id, isRequired, } = useInput({
        disabled,
        format,
        parse,
        readOnly,
        source,
        validate,
        ...rest,
    });
    const renderHelperText = helperText !== false || invalid;
    return (React.createElement(StyledAutocomplete, { multiple: true, freeSolo: true, autoSelect: true, options: [], id: id, className: clsx('ra-input', `ra-input-${source}`, className), renderTags: (value, getTagProps) => value.map((option, index) => {
            const { key, ...tagProps } = getTagProps({ index });
            return (React.createElement(Chip, { size: "small", label: option, key: key, ...tagProps }));
        }), renderInput: params => (React.createElement(TextField, { ...params, label: label !== '' && label !== false ? (React.createElement(FieldTitle, { label: label, source: source, resource: props.resource, isRequired: isRequired })) : null, helperText: renderHelperText ? (React.createElement(InputHelperText, { error: error?.message, helperText: helperText })) : null, error: invalid, variant: variant, margin: margin, size: size })), sx: sx, ...field, value: field.value || emptyArray, onChange: (e, newValue) => field.onChange(newValue), ...rest, disabled: disabled || readOnly }));
};
const emptyArray = [];
const PREFIX = 'RaTextArrayInput';
const StyledAutocomplete = styled((Autocomplete), {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})(({ theme }) => ({
    minWidth: theme.spacing(20),
}));
//# sourceMappingURL=TextArrayInput.js.map