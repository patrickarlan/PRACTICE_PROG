import * as React from 'react';
import { useCallback } from 'react';
import clsx from 'clsx';
import FormControlLabel from '@mui/material/FormControlLabel';
import FormHelperText from '@mui/material/FormHelperText';
import FormGroup from '@mui/material/FormGroup';
import Switch from '@mui/material/Switch';
import { FieldTitle, useInput } from 'ra-core';
import { styled, useThemeProps, } from '@mui/material/styles';
import { sanitizeInputRestProps } from "./sanitizeInputRestProps.js";
import { InputHelperText } from "./InputHelperText.js";
export const BooleanInput = (props) => {
    const { className, row = false, defaultValue = false, format, label, fullWidth, helperText, onBlur, onChange, onFocus, readOnly, disabled, parse, resource, source, validate, options = defaultOptions, sx, ...rest } = useThemeProps({
        props: props,
        name: PREFIX,
    });
    const { id, field, isRequired, fieldState: { error, invalid }, } = useInput({
        defaultValue,
        format,
        parse,
        resource,
        source,
        onBlur,
        onChange,
        type: 'checkbox',
        validate,
        disabled,
        readOnly,
        ...rest,
    });
    const handleChange = useCallback(event => {
        field.onChange(event);
        // Ensure field is considered as touched
        field.onBlur();
    }, [field]);
    const renderHelperText = helperText !== false || invalid;
    return (React.createElement(StyledFormGroup, { className: clsx('ra-input', `ra-input-${source}`, className), row: row, sx: sx },
        React.createElement(FormControlLabel, { inputRef: field.ref, control: React.createElement(Switch, { id: id, name: field.name, onChange: handleChange, onFocus: onFocus, checked: Boolean(field.value), ...sanitizeInputRestProps(rest), ...options, disabled: disabled || readOnly, readOnly: readOnly }), label: React.createElement(FieldTitle, { label: label, source: source, resource: resource, isRequired: isRequired }) }),
        renderHelperText ? (React.createElement(FormHelperText, { error: invalid },
            React.createElement(InputHelperText, { error: error?.message, helperText: helperText }))) : null));
};
const defaultOptions = {};
const PREFIX = 'RaBooleanInput';
const StyledFormGroup = styled(FormGroup, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=BooleanInput.js.map