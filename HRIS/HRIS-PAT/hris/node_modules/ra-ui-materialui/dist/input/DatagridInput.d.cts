import * as React from 'react';
import { ReactElement, ReactNode } from 'react';
import { ChoicesProps, SupportCreateSuggestionOptions } from 'ra-core';
import { ComponentsOverrides } from '@mui/material/styles';
import { CommonInputProps } from "./CommonInputProps.cjs";
import { DatagridProps } from "../list/index.cjs";
/**
 * WIP: This component is not yet ready to be used.
 *
 * An input for selecting items displayed in a datagrid
 *
 * @example
 * const membersFilters = [
 *     <TextInput label="Search" source="q" alwaysOn />,
 * ];
 * const TeamEdit = () => (
 *    <Edit>
 *        <SimpleForm>
 *            <TextInput source="name" />
 *            <ReferenceArrayInput
 *                source="members"
 *                reference="users"
 *                filter={{ is_retired: false }}
 *                perPage={50}
 *                sort={{ field: 'lastName', order: 'ASC' }}
 *            >
 *                <DatagridInput
 *                    filters={membersFilters}
 *                >
 *                    <TextField source="firstName" />
 *                    <TextField source="lastName" />
 *                </DatagridInput>
 *            </ReferenceArrayInput>
 *        </SimpleForm>
 *    </Edit>
 * );
 */
export declare const DatagridInput: (inProps: DatagridInputProps) => React.JSX.Element;
export type DatagridInputProps = Omit<CommonInputProps, 'fullWidth' | 'source' | 'readOnly' | 'disabled'> & Omit<ChoicesProps, 'disableValue'> & Omit<SupportCreateSuggestionOptions, 'handleChange'> & DatagridProps & {
    children?: ReactNode;
    source?: string;
    filters?: ReactElement | ReactElement[];
    pagination?: ReactElement | false;
};
declare const PREFIX = "RaDatagridInput";
declare module '@mui/material/styles' {
    interface ComponentNameToClassKey {
        [PREFIX]: 'root';
    }
    interface ComponentsPropsList {
        [PREFIX]: Partial<DatagridInputProps>;
    }
    interface Components {
        [PREFIX]?: {
            defaultProps?: ComponentsPropsList[typeof PREFIX];
            styleOverrides?: ComponentsOverrides<Omit<Theme, 'components'>>[typeof PREFIX];
        };
    }
}
export {};
//# sourceMappingURL=DatagridInput.d.ts.map