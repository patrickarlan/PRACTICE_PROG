import * as React from 'react';
import AddIcon from '@mui/icons-material/AddCircleOutline';
import clsx from 'clsx';
import { useSimpleFormIterator } from 'ra-core';
import { IconButtonWithTooltip } from "../../button/index.js";
export const AddItemButton = (props) => {
    const { add, source } = useSimpleFormIterator();
    const { className, ...rest } = props;
    return (React.createElement(IconButtonWithTooltip, { label: "ra.action.add", size: "small", onClick: () => add(), color: "primary", className: clsx(`button-add button-add-${source}`, className), ...rest },
        React.createElement(AddIcon, { fontSize: "small" })));
};
//# sourceMappingURL=AddItemButton.js.map