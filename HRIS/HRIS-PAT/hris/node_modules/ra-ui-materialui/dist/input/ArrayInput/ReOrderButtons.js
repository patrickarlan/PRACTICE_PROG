import * as React from 'react';
import clsx from 'clsx';
import { useSimpleFormIterator, useSimpleFormIteratorItem } from 'ra-core';
import ArrowUpwardIcon from '@mui/icons-material/ArrowCircleUp';
import ArrowDownwardIcon from '@mui/icons-material/ArrowCircleDown';
import { IconButtonWithTooltip } from "../../button/index.js";
export const ReOrderButtons = ({ className }) => {
    const { index, total, reOrder } = useSimpleFormIteratorItem();
    const { source } = useSimpleFormIterator();
    return (React.createElement("span", { className: clsx(`button-reorder button-reorder-${source}-${index}`, className) },
        React.createElement(IconButtonWithTooltip, { label: "ra.action.move_up", size: "small", onClick: () => reOrder(index - 1), disabled: index <= 0, color: "primary" },
            React.createElement(ArrowUpwardIcon, { fontSize: "small" })),
        React.createElement(IconButtonWithTooltip, { label: "ra.action.move_down", size: "small", onClick: () => reOrder(index + 1), disabled: total == null || index >= total - 1, color: "primary" },
            React.createElement(ArrowDownwardIcon, { fontSize: "small" }))));
};
//# sourceMappingURL=ReOrderButtons.js.map