import * as React from 'react';
import CloseIcon from '@mui/icons-material/RemoveCircleOutline';
import clsx from 'clsx';
import { useSimpleFormIterator, useSimpleFormIteratorItem } from 'ra-core';
import { IconButtonWithTooltip } from "../../button/index.js";
export const RemoveItemButton = (props) => {
    const { remove, index } = useSimpleFormIteratorItem();
    const { source } = useSimpleFormIterator();
    const { className, ...rest } = props;
    return (React.createElement(IconButtonWithTooltip, { label: "ra.action.remove", size: "small", onClick: () => remove(), color: "warning", className: clsx(`button-remove button-remove-${source}-${index}`, className), ...rest },
        React.createElement(CloseIcon, { fontSize: "small" })));
};
//# sourceMappingURL=RemoveItemButton.js.map