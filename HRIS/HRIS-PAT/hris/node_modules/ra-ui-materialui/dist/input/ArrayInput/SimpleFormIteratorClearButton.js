import * as React from 'react';
import { useState } from 'react';
import { useEvent, useSimpleFormIterator, useTranslate, } from 'ra-core';
import { Confirm } from "../../layout/Confirm.js";
import { ClearArrayButton } from "./ClearArrayButton.js";
export const SimpleFormIteratorClearButton = ({ className, disableClear, disableRemove, }) => {
    const translate = useTranslate();
    const [confirmIsOpen, setConfirmIsOpen] = useState(false);
    const { clear, total } = useSimpleFormIterator();
    const handleArrayClear = useEvent(() => {
        clear();
        setConfirmIsOpen(false);
    });
    if (total === 0 || disableClear === true || disableRemove === true) {
        return null;
    }
    return (React.createElement(React.Fragment, null,
        React.createElement(Confirm, { isOpen: confirmIsOpen, title: translate('ra.action.clear_array_input'), content: translate('ra.message.clear_array_input'), onConfirm: handleArrayClear, onClose: () => setConfirmIsOpen(false) }),
        React.createElement(ClearArrayButton, { className: className, onClick: () => setConfirmIsOpen(true) })));
};
//# sourceMappingURL=SimpleFormIteratorClearButton.js.map