import * as React from 'react';
import { CoreAdminContext } from 'ra-core';
import { ThemeProvider, ThemesContext, defaultLightTheme, defaultDarkTheme, } from "./theme/index.js";
export const AdminContext = (props) => {
    const { theme, lightTheme = defaultLightTheme, darkTheme, defaultTheme, children, ...rest } = props;
    return (React.createElement(CoreAdminContext, { ...rest },
        React.createElement(ThemesContext.Provider, { value: {
                lightTheme: theme || lightTheme,
                darkTheme: theme && !darkTheme
                    ? undefined
                    : !darkTheme && darkTheme !== null
                        ? defaultDarkTheme
                        : darkTheme ?? undefined,
                defaultTheme,
            } },
            React.createElement(ThemeProvider, null, children))));
};
AdminContext.displayName = 'AdminContext';
//# sourceMappingURL=AdminContext.js.map