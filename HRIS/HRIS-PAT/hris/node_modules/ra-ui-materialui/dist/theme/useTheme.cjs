"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.useTheme = void 0;
const ra_core_1 = require("ra-core");
const material_1 = require("@mui/material");
const useThemesContext_1 = require("./useThemesContext.cjs");
/**
 * Read and update the theme mode (light or dark)
 *
 * @example
 * const [theme, setTheme] = useTheme('light');
 * const toggleTheme = () => {
 *    setTheme(theme === 'light' ? 'dark' : 'light');
 * };
 *
 */
const useTheme = (type) => {
    const { darkTheme } = (0, useThemesContext_1.useThemesContext)();
    const prefersDarkMode = (0, material_1.useMediaQuery)('(prefers-color-scheme: dark)', {
        noSsr: true,
    });
    const [theme, setter] = (0, ra_core_1.useStore)('theme', type ?? (prefersDarkMode && darkTheme ? 'dark' : 'light'));
    // Ensure that even though the store has its value set to 'dark', we still use the light theme when no dark theme is available
    return [darkTheme != null ? theme : 'light', setter];
};
exports.useTheme = useTheme;
//# sourceMappingURL=useTheme.js.map