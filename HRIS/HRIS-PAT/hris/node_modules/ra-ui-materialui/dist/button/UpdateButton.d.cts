import * as React from 'react';
import { UpdateWithConfirmButtonProps } from "./UpdateWithConfirmButton.cjs";
import { UpdateWithUndoButtonProps } from "./UpdateWithUndoButton.cjs";
/**
 * Updates the current record.
 *
 * To be used inside the <Edit actions> prop or <Show actions> prop.
 *
 * @example // basic usage
 * import * as React from 'react';
 * import { Edit, TopToolbar, UpdateButton } from 'react-admin';
 *
 * const PostEditActions = () => (
 *     <TopToolbar>
 *         <UpdateButton label="Reset Views" data={{ views: 0 }} />
 *     </TopToolbar>
 * );
 *
 * export const PostEdit = () => (
 *     <Edit actions={<PostEditActions />}>
 *         ...
 *     </Edit>
 * );
 */
export declare const UpdateButton: React.ForwardRefExoticComponent<(Omit<{
    mutationMode?: "undoable";
} & UpdateWithUndoButtonProps<any, unknown>, "ref"> | Omit<{
    mutationMode?: "pessimistic" | "optimistic";
} & UpdateWithConfirmButtonProps<any, Error>, "ref">) & React.RefAttributes<HTMLButtonElement>>;
export type UpdateButtonProps = ({
    mutationMode?: 'undoable';
} & UpdateWithUndoButtonProps) | ({
    mutationMode?: 'pessimistic' | 'optimistic';
} & UpdateWithConfirmButtonProps);
declare const PREFIX = "RaUpdateButton";
declare module '@mui/material/styles' {
    interface ComponentsPropsList {
        [PREFIX]: Partial<UpdateButtonProps>;
    }
    interface Components {
        [PREFIX]?: {
            defaultProps?: ComponentsPropsList[typeof PREFIX];
        };
    }
}
export {};
//# sourceMappingURL=UpdateButton.d.ts.map