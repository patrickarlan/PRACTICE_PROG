import * as React from 'react';
import { MutationMode } from 'ra-core';
import { BulkDeleteWithConfirmButtonProps } from "./BulkDeleteWithConfirmButton.cjs";
import { BulkDeleteWithUndoButtonProps } from "./BulkDeleteWithUndoButton.cjs";
/**
 * Deletes the selected rows.
 *
 * To be used inside the <Datagrid bulkActionButtons> prop (where it's enabled by default).
 *
 * @example // basic usage
 * import { BulkDeleteButton, BulkExportButton, List, Datagrid } from 'react-admin';
 *
 * const PostBulkActionButtons = () => (
 *     <>
 *         <BulkExportButton />
 *         <BulkDeleteButton />
 *     </>
 * );
 *
 * export const PostList = () => (
 *     <List>
 *        <Datagrid bulkActionButtons={<PostBulkActionButtons />}>
 *             ...
 *       </Datagrid>
 *     </List>
 * );
 */
export declare const BulkDeleteButton: React.ForwardRefExoticComponent<(Omit<Props & BulkDeleteWithUndoButtonProps<any, unknown>, "ref"> | Omit<Props & BulkDeleteWithConfirmButtonProps<any, unknown>, "ref">) & React.RefAttributes<HTMLButtonElement>>;
interface Props {
    mutationMode?: MutationMode;
}
export type BulkDeleteButtonProps = Props & (BulkDeleteWithUndoButtonProps | BulkDeleteWithConfirmButtonProps);
declare const PREFIX = "RaBulkDeleteButton";
declare module '@mui/material/styles' {
    interface ComponentsPropsList {
        [PREFIX]: Partial<BulkDeleteButtonProps>;
    }
    interface Components {
        [PREFIX]?: {
            defaultProps?: ComponentsPropsList[typeof PREFIX];
        };
    }
}
export {};
//# sourceMappingURL=BulkDeleteButton.d.ts.map