import * as React from 'react';
import { useCallback } from 'react';
import DownloadIcon from '@mui/icons-material/GetApp';
import { fetchRelatedRecords, useDataProvider, useNotify, useListContext, } from 'ra-core';
import { Button } from "./Button.js";
export const ExportButton = React.forwardRef(function ExportButton(props, ref) {
    const { maxResults = 1000, onClick, label = 'ra.action.export', icon = defaultIcon, exporter: customExporter, meta, ...rest } = props;
    const { filter, filterValues, resource, sort, exporter: exporterFromContext, total, getData, } = useListContext();
    const exporter = customExporter || exporterFromContext;
    const dataProvider = useDataProvider();
    const notify = useNotify();
    const handleClick = useCallback(event => {
        const fetchData = getData
            ? getData({ maxResults, meta })
            : dataProvider
                .getList(resource, {
                sort,
                filter: filter
                    ? { ...filterValues, ...filter }
                    : filterValues,
                pagination: { page: 1, perPage: maxResults },
                meta,
            })
                .then(({ data }) => data);
        Promise.resolve(fetchData)
            .then(data => exporter &&
            exporter(data, fetchRelatedRecords(dataProvider), dataProvider, resource))
            .catch(error => {
            console.error(error);
            notify('ra.notification.http_error', { type: 'error' });
        });
        if (typeof onClick === 'function') {
            onClick(event);
        }
    }, [
        dataProvider,
        exporter,
        filter,
        filterValues,
        getData,
        maxResults,
        notify,
        onClick,
        resource,
        sort,
        meta,
    ]);
    return (React.createElement(Button, { ref: ref, onClick: handleClick, label: label, disabled: total === 0, ...sanitizeRestProps(rest) }, icon));
});
const defaultIcon = React.createElement(DownloadIcon, null);
const sanitizeRestProps = ({ resource, ...rest }) => rest;
//# sourceMappingURL=ExportButton.js.map