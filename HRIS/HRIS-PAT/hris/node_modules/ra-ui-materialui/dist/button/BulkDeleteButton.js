import * as React from 'react';
import { useCanAccess, useResourceContext } from 'ra-core';
import { useThemeProps } from '@mui/material/styles';
import { BulkDeleteWithConfirmButton, } from "./BulkDeleteWithConfirmButton.js";
import { BulkDeleteWithUndoButton, } from "./BulkDeleteWithUndoButton.js";
/**
 * Deletes the selected rows.
 *
 * To be used inside the <Datagrid bulkActionButtons> prop (where it's enabled by default).
 *
 * @example // basic usage
 * import { BulkDeleteButton, BulkExportButton, List, Datagrid } from 'react-admin';
 *
 * const PostBulkActionButtons = () => (
 *     <>
 *         <BulkExportButton />
 *         <BulkDeleteButton />
 *     </>
 * );
 *
 * export const PostList = () => (
 *     <List>
 *        <Datagrid bulkActionButtons={<PostBulkActionButtons />}>
 *             ...
 *       </Datagrid>
 *     </List>
 * );
 */
export const BulkDeleteButton = React.forwardRef(function BulkDeleteButton(inProps, ref) {
    const { mutationMode = 'undoable', ...props } = useThemeProps({
        name: PREFIX,
        props: inProps,
    });
    const resource = useResourceContext(props);
    if (!resource) {
        throw new Error('<BulkDeleteButton> components should be used inside a <Resource> component or provided with a resource prop.');
    }
    const { canAccess, isPending } = useCanAccess({
        action: 'delete',
        resource,
    });
    if (!canAccess || isPending) {
        return null;
    }
    return mutationMode === 'undoable' ? (React.createElement(BulkDeleteWithUndoButton, { ref: ref, ...props })) : (React.createElement(BulkDeleteWithConfirmButton, { ref: ref, mutationMode: mutationMode, ...props }));
});
const PREFIX = 'RaBulkDeleteButton';
//# sourceMappingURL=BulkDeleteButton.js.map