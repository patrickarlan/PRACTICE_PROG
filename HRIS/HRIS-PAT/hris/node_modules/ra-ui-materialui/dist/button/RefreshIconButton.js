import * as React from 'react';
import { useCallback } from 'react';
import Tooltip from '@mui/material/Tooltip';
import IconButton from '@mui/material/IconButton';
import NavigationRefresh from '@mui/icons-material/Refresh';
import { useRefresh, useTranslate } from 'ra-core';
export const RefreshIconButton = React.forwardRef(function RefreshIconButton(props, ref) {
    const { label = 'ra.action.refresh', icon = defaultIcon, onClick, className, ...rest } = props;
    const refresh = useRefresh();
    const translate = useTranslate();
    const handleClick = useCallback(event => {
        event.preventDefault();
        refresh();
        if (typeof onClick === 'function') {
            onClick(event);
        }
    }, [refresh, onClick]);
    return (React.createElement(Tooltip, { title: label && translate(label, { _: 'Refresh' }) },
        React.createElement(IconButton, { ref: ref, "aria-label": label && translate(label, { _: 'Refresh' }), className: className, color: "inherit", onClick: handleClick, ...rest }, icon)));
});
const defaultIcon = React.createElement(NavigationRefresh, null);
//# sourceMappingURL=RefreshIconButton.js.map