import * as React from 'react';
import { UpdateWithConfirmButton, } from "./UpdateWithConfirmButton.js";
import { UpdateWithUndoButton, } from "./UpdateWithUndoButton.js";
import { useThemeProps } from '@mui/material/styles';
/**
 * Updates the current record.
 *
 * To be used inside the <Edit actions> prop or <Show actions> prop.
 *
 * @example // basic usage
 * import * as React from 'react';
 * import { Edit, TopToolbar, UpdateButton } from 'react-admin';
 *
 * const PostEditActions = () => (
 *     <TopToolbar>
 *         <UpdateButton label="Reset Views" data={{ views: 0 }} />
 *     </TopToolbar>
 * );
 *
 * export const PostEdit = () => (
 *     <Edit actions={<PostEditActions />}>
 *         ...
 *     </Edit>
 * );
 */
export const UpdateButton = React.forwardRef(function UpdateButton(props, ref) {
    const { mutationMode = 'undoable', ...rest } = useThemeProps({
        name: PREFIX,
        props: props,
    });
    return mutationMode === 'undoable' ? (React.createElement(UpdateWithUndoButton, { ref: ref, ...rest })) : (React.createElement(UpdateWithConfirmButton, { ref: ref, mutationMode: mutationMode, ...rest }));
});
const PREFIX = 'RaUpdateButton';
//# sourceMappingURL=UpdateButton.js.map