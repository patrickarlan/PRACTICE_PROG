import * as React from 'react';
import { LinkBase } from 'ra-core';
import { ComponentsOverrides } from '@mui/material/styles';
import { type ButtonProps } from "./Button.cjs";
/**
 * Opens the List view of a given resource
 *
 * @example // basic usage
 * import { ListButton } from 'react-admin';
 *
 * const CommentListButton = () => (
 *     <ListButton label="Comments" />
 * );
 *
 * @example // linking back to the list from the Edit view
 * import { TopToolbar, ListButton, ShowButton, Edit } from 'react-admin';
 *
 * const PostEditActions = () => (
 *     <TopToolbar>
 *         <ListButton />
 *         <ShowButton />
 *     </TopToolbar>
 * );
 *
 * export const PostEdit = (props) => (
 *     <Edit actions={<PostEditActions />} {...props}>
 *         ...
 *     </Edit>
 * );
 */
export declare const ListButton: React.ForwardRefExoticComponent<Omit<ListButtonProps, "ref"> & React.RefAttributes<HTMLAnchorElement>>;
interface Props {
    icon?: React.ReactNode;
    label?: string;
    resource?: string;
    scrollToTop?: boolean;
}
export type ListButtonProps = Props & Omit<ButtonProps<typeof LinkBase>, 'to'>;
declare const PREFIX = "RaListButton";
declare module '@mui/material/styles' {
    interface ComponentNameToClassKey {
        [PREFIX]: 'root';
    }
    interface ComponentsPropsList {
        [PREFIX]: Partial<ListButtonProps>;
    }
    interface Components {
        [PREFIX]?: {
            defaultProps?: ComponentsPropsList[typeof PREFIX];
            styleOverrides?: ComponentsOverrides<Omit<Theme, 'components'>>[typeof PREFIX];
        };
    }
}
export {};
//# sourceMappingURL=ListButton.d.ts.map