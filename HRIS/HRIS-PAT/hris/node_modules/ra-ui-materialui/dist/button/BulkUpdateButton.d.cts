import * as React from 'react';
import { MutationMode } from 'ra-core';
import { BulkUpdateWithConfirmButtonProps } from "./BulkUpdateWithConfirmButton.cjs";
import { BulkUpdateWithUndoButtonProps } from "./BulkUpdateWithUndoButton.cjs";
/**
 * Updates the selected rows.
 *
 * To be used inside the <Datagrid bulkActionButtons> prop (where it's enabled by default).
 *
 * @example // basic usage
 * import { BulkUpdateButton, BulkExportButton, List, Datagrid } from 'react-admin';
 *
 * const PostBulkActionButtons = () => (
 *     <>
 *         <BulkExportButton />
 *         <BulkUpdateButton label="Reset Views" data={{ views: 0 }} />
 *     </>
 * );
 *
 * export const PostList = () => (
 *     <List>
 *        <Datagrid bulkActionButtons={<PostBulkActionButtons />}>
 *          ...
 *        </Datagrid>
 *     </List>
 * );
 */
export declare const BulkUpdateButton: React.ForwardRefExoticComponent<(Omit<Props & BulkUpdateWithUndoButtonProps<any, unknown>, "ref"> | Omit<Props & BulkUpdateWithConfirmButtonProps<any, unknown>, "ref">) & React.RefAttributes<HTMLButtonElement>>;
interface Props {
    mutationMode?: MutationMode;
}
export type BulkUpdateButtonProps = Props & (BulkUpdateWithUndoButtonProps | BulkUpdateWithConfirmButtonProps);
declare const PREFIX = "RaBulkUpdateButton";
declare module '@mui/material/styles' {
    interface ComponentsPropsList {
        [PREFIX]: Partial<BulkUpdateButtonProps>;
    }
    interface Components {
        [PREFIX]?: {
            defaultProps?: ComponentsPropsList[typeof PREFIX];
        };
    }
}
export {};
//# sourceMappingURL=BulkUpdateButton.d.ts.map