import * as React from 'react';
import ActionDelete from '@mui/icons-material/Delete';
import { styled, useThemeProps, } from '@mui/material/styles';
import { useBulkDeleteController, } from 'ra-core';
import { Button } from "./Button.js";
export const BulkDeleteWithUndoButton = React.forwardRef(function BulkDeleteWithUndoButton(inProps, ref) {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { label = 'ra.action.delete', icon = defaultIcon, onClick, ...rest } = props;
    const { handleDelete, isPending } = useBulkDeleteController(rest);
    const handleClick = e => {
        handleDelete();
        if (typeof onClick === 'function') {
            onClick(e);
        }
    };
    return (React.createElement(StyledButton, { ref: ref, onClick: handleClick, label: label, disabled: isPending, color: "error", ...sanitizeRestProps(rest) }, icon));
});
const defaultIcon = React.createElement(ActionDelete, null);
const sanitizeRestProps = ({ classes, label, resource, successMessage, ...rest }) => rest;
const PREFIX = 'RaBulkDeleteWithUndoButton';
const StyledButton = styled(Button, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})(() => ({}));
//# sourceMappingURL=BulkDeleteWithUndoButton.js.map