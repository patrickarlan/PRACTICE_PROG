"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ExportButton = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const GetApp_1 = __importDefault(require("@mui/icons-material/GetApp"));
const ra_core_1 = require("ra-core");
const Button_1 = require("./Button.cjs");
exports.ExportButton = React.forwardRef(function ExportButton(props, ref) {
    const { maxResults = 1000, onClick, label = 'ra.action.export', icon = defaultIcon, exporter: customExporter, meta, ...rest } = props;
    const { filter, filterValues, resource, sort, exporter: exporterFromContext, total, getData, } = (0, ra_core_1.useListContext)();
    const exporter = customExporter || exporterFromContext;
    const dataProvider = (0, ra_core_1.useDataProvider)();
    const notify = (0, ra_core_1.useNotify)();
    const handleClick = (0, react_1.useCallback)(event => {
        const fetchData = getData
            ? getData({ maxResults, meta })
            : dataProvider
                .getList(resource, {
                sort,
                filter: filter
                    ? { ...filterValues, ...filter }
                    : filterValues,
                pagination: { page: 1, perPage: maxResults },
                meta,
            })
                .then(({ data }) => data);
        Promise.resolve(fetchData)
            .then(data => exporter &&
            exporter(data, (0, ra_core_1.fetchRelatedRecords)(dataProvider), dataProvider, resource))
            .catch(error => {
            console.error(error);
            notify('ra.notification.http_error', { type: 'error' });
        });
        if (typeof onClick === 'function') {
            onClick(event);
        }
    }, [
        dataProvider,
        exporter,
        filter,
        filterValues,
        getData,
        maxResults,
        notify,
        onClick,
        resource,
        sort,
        meta,
    ]);
    return (React.createElement(Button_1.Button, { ref: ref, onClick: handleClick, label: label, disabled: total === 0, ...sanitizeRestProps(rest) }, icon));
});
const defaultIcon = React.createElement(GetApp_1.default, null);
const sanitizeRestProps = ({ resource, ...rest }) => rest;
//# sourceMappingURL=ExportButton.js.map