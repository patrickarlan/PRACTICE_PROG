import * as React from 'react';
import ActionUpdate from '@mui/icons-material/Update';
import { styled, useThemeProps, } from '@mui/material/styles';
import { useBulkUpdateController, } from 'ra-core';
import { Button } from "./Button.js";
export const BulkUpdateWithUndoButton = React.forwardRef(function BulkUpdateWithUndoButton(inProps, ref) {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { data, label = 'ra.action.update', icon = defaultIcon, onClick, ...rest } = props;
    const { handleUpdate, isPending } = useBulkUpdateController(rest);
    const handleClick = (e) => {
        e.stopPropagation();
        handleUpdate(data);
        if (typeof onClick === 'function') {
            onClick(e);
        }
    };
    return (React.createElement(StyledButton, { ref: ref, onClick: handleClick, label: label, disabled: isPending, ...sanitizeRestProps(rest) }, icon));
});
const defaultIcon = React.createElement(ActionUpdate, null);
const sanitizeRestProps = ({ label, resource, successMessage, ...rest }) => rest;
const PREFIX = 'RaBulkUpdateWithUndoButton';
const StyledButton = styled(Button, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})(() => ({}));
//# sourceMappingURL=BulkUpdateWithUndoButton.js.map