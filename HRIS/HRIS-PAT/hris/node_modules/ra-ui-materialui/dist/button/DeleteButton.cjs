"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.DeleteButton = void 0;
const React = __importStar(require("react"));
const ra_core_1 = require("ra-core");
const styles_1 = require("@mui/material/styles");
const DeleteWithUndoButton_1 = require("./DeleteWithUndoButton.cjs");
const DeleteWithConfirmButton_1 = require("./DeleteWithConfirmButton.cjs");
/**
 * Button used to delete a single record. Added by default by the <Toolbar> of edit and show views.
 *
 * @typedef {Object} Props The props you can use (other props are injected if you used it in the <Toolbar>)
 * @prop {boolean} mutationMode Either 'pessimistic', 'optimistic' or 'undoable'. Determine whether the deletion uses an undo button in a notification or a confirmation dialog. Defaults to 'undoable'.
 * @prop {Object} record The current resource record
 * @prop {string} className
 * @prop {string} label Button label. Defaults to 'ra.action.delete, translated.
 * @prop {boolean} disabled Disable the button.
 * @prop {string} variant Material UI variant for the button. Defaults to 'contained'.
 * @prop {ReactNode} icon Override the icon. Defaults to the Delete icon from Material UI.
 *
 * @param {Props} inProps
 *
 * @example Usage in the <TopToolbar> of an <Edit> form
 *
 * import * as React from 'react';
 * import { Edit, DeleteButton, TopToolbar } from 'react-admin';
 *
 * const EditActions = props => {
 *     const { data, resource } = props;
 *     return (
 *         <TopToolbar>
 *             <DeleteButton
 *                 mutationMode="pessimistic" // Renders the <DeleteWithConfirmButton>
 *             />
 *         </TopToolbar>
 *     );
 * };
 *
 * const Edit = props => {
 *     return <Edit actions={<EditActions />} {...props} />;
 * };
 */
exports.DeleteButton = React.forwardRef(function DeleteButton(inProps, ref) {
    const props = (0, styles_1.useThemeProps)({
        name: PREFIX,
        props: inProps,
    });
    const { mutationMode, ...rest } = props;
    const record = (0, ra_core_1.useRecordContext)(props);
    const resource = (0, ra_core_1.useResourceContext)(props);
    if (!resource) {
        throw new Error('<DeleteButton> components should be used inside a <Resource> component or provided the resource prop.');
    }
    const { canAccess, isPending } = (0, ra_core_1.useCanAccess)({
        action: 'delete',
        resource,
        record,
    });
    const saveContext = (0, ra_core_1.useSaveContext)(props);
    if (!record || record.id == null || !canAccess || isPending) {
        return null;
    }
    const finalMutationMode = mutationMode
        ? mutationMode
        : saveContext?.mutationMode
            ? saveContext.mutationMode
            : 'undoable';
    return finalMutationMode === 'undoable' ? (React.createElement(DeleteWithUndoButton_1.DeleteWithUndoButton, { ref: ref, record: record, ...rest })) : (React.createElement(DeleteWithConfirmButton_1.DeleteWithConfirmButton
    // @ts-ignore I looked for the error for one hour without finding it
    , { 
        // @ts-ignore I looked for the error for one hour without finding it
        mutationMode: finalMutationMode, ref: ref, record: record, ...rest }));
});
const PREFIX = 'RaDeleteButton';
//# sourceMappingURL=DeleteButton.js.map