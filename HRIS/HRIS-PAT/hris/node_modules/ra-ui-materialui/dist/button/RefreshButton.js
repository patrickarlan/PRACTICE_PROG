import * as React from 'react';
import { useCallback } from 'react';
import NavigationRefresh from '@mui/icons-material/Refresh';
import { useRefresh } from 'ra-core';
import { styled, useThemeProps, } from '@mui/material/styles';
import { Button } from "./Button.js";
export const RefreshButton = React.forwardRef(function RefreshButton(inProps, ref) {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { label = 'ra.action.refresh', icon = defaultIcon, onClick, ...rest } = props;
    const refresh = useRefresh();
    const handleClick = useCallback(event => {
        event.preventDefault();
        refresh();
        if (typeof onClick === 'function') {
            onClick(event);
        }
    }, [refresh, onClick]);
    return (React.createElement(StyledButton, { ref: ref, label: label, onClick: handleClick, ...rest }, icon));
});
const defaultIcon = React.createElement(NavigationRefresh, null);
const PREFIX = 'RaRefreshButton';
const StyledButton = styled(Button, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=RefreshButton.js.map