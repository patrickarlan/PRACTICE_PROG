import * as React from 'react';
import { IconButton, Tooltip } from '@mui/material';
import { useTranslate } from 'ra-core';
/**
 * An IconButton with a tooltip which ensures the tooltip is closed on click to avoid ghost tooltips
 * when the button position changes.
 */
export const IconButtonWithTooltip = ({ label, onClick, ...props }) => {
    const translate = useTranslate();
    const [open, setOpen] = React.useState(false);
    const handleClose = () => {
        setOpen(false);
    };
    const handleOpen = () => {
        setOpen(true);
    };
    let translatedLabel = label;
    if (typeof label === 'string') {
        translatedLabel = translate(label, { _: label });
    }
    const handleClick = (event) => {
        handleClose();
        onClick && onClick(event);
    };
    return (React.createElement(Tooltip, { title: translatedLabel, open: open, onOpen: handleOpen, onClose: handleClose },
        React.createElement(IconButton, { "aria-label": typeof translatedLabel === 'string'
                ? translatedLabel
                : undefined, onClick: handleClick, ...props })));
};
//# sourceMappingURL=IconButtonWithTooltip.js.map