"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.SelectPageCheckbox = void 0;
const material_1 = require("@mui/material");
const ra_core_1 = require("ra-core");
const React = __importStar(require("react"));
const react_1 = require("react");
const SelectPageCheckbox = () => {
    const data = (0, ra_core_1.useDataTableDataContext)();
    const { isRowSelectable, onSelect } = (0, ra_core_1.useDataTableCallbacksContext)();
    const selectedIds = (0, ra_core_1.useDataTableSelectedIdsContext)();
    const translate = (0, ra_core_1.useTranslate)();
    const handleSelectAll = (0, react_1.useCallback)(event => {
        if (!onSelect || !selectedIds || !data)
            return;
        onSelect(event.target.checked
            ? selectedIds.concat(data
                .filter(record => !selectedIds.includes(record.id) &&
                (!isRowSelectable ||
                    isRowSelectable(record)))
                .map(record => record.id))
            : // We should only unselect the ids present in the current page
                selectedIds.filter(id => !data.some(record => record.id === id)));
    }, [data, onSelect, isRowSelectable, selectedIds]);
    const selectableIds = Array.isArray(data)
        ? isRowSelectable
            ? data
                .filter(record => isRowSelectable(record))
                .map(record => record.id)
            : data.map(record => record.id)
        : [];
    return (React.createElement(material_1.Checkbox, { inputProps: {
            'aria-label': translate('ra.action.select_all', {
                _: 'Select all',
            }),
        }, className: "select-all", color: "primary", checked: selectedIds &&
            selectedIds.length > 0 &&
            selectableIds.length > 0 &&
            selectableIds.every(id => selectedIds.includes(id)), onChange: handleSelectAll, onClick: e => e.stopPropagation() }));
};
exports.SelectPageCheckbox = SelectPageCheckbox;
//# sourceMappingURL=SelectPageCheckbox.js.map