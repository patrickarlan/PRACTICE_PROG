"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ColumnsSelectorItem = void 0;
const React = __importStar(require("react"));
const ra_core_1 = require("ra-core");
const diacritic = __importStar(require("diacritic"));
const preferences_1 = require("../../preferences/index.cjs");
const ColumnsSelectorItem = ({ source, label, }) => {
    const resource = (0, ra_core_1.useResourceContext)();
    const { storeKey, defaultHiddenColumns } = (0, ra_core_1.useDataTableStoreContext)();
    const [hiddenColumns, setHiddenColumns] = (0, ra_core_1.useStore)(storeKey, defaultHiddenColumns);
    const columnRank = (0, ra_core_1.useDataTableColumnRankContext)();
    const [columnRanks, setColumnRanks] = (0, ra_core_1.useStore)(`${storeKey}_columnRanks`);
    const columnFilter = (0, ra_core_1.useDataTableColumnFilterContext)();
    const translateLabel = (0, ra_core_1.useTranslateLabel)();
    if (!source && (!label || typeof label !== 'string'))
        return null;
    const sourceOrLabel = source || label;
    const fieldLabel = translateLabel({
        label: typeof label === 'string' ? label : undefined,
        resource,
        source,
    });
    const isColumnHidden = hiddenColumns.includes(sourceOrLabel);
    const isColumnFiltered = fieldLabelMatchesFilter(fieldLabel, columnFilter);
    const handleMove = (index1, index2) => {
        const colRanks = !columnRanks
            ? padRanks([], Math.max(index1, index2) + 1)
            : Math.max(index1, index2) > columnRanks.length - 1
                ? padRanks(columnRanks, Math.max(index1, index2) + 1)
                : columnRanks;
        const index1Pos = colRanks.findIndex(
        // eslint-disable-next-line eqeqeq
        index => index == index1);
        const index2Pos = colRanks.findIndex(
        // eslint-disable-next-line eqeqeq
        index => index == index2);
        if (index1Pos === -1 || index2Pos === -1) {
            return;
        }
        let newColumnRanks;
        if (index1Pos > index2Pos) {
            newColumnRanks = [
                ...colRanks.slice(0, index2Pos),
                colRanks[index1Pos],
                ...colRanks.slice(index2Pos, index1Pos),
                ...colRanks.slice(index1Pos + 1),
            ];
        }
        else {
            newColumnRanks = [
                ...colRanks.slice(0, index1Pos),
                ...colRanks.slice(index1Pos + 1, index2Pos + 1),
                colRanks[index1Pos],
                ...colRanks.slice(index2Pos + 1),
            ];
        }
        setColumnRanks(newColumnRanks);
    };
    return isColumnFiltered ? (React.createElement(preferences_1.FieldToggle, { key: columnRank, source: sourceOrLabel, label: fieldLabel, index: String(columnRank), selected: !isColumnHidden, onToggle: () => isColumnHidden
            ? setHiddenColumns(hiddenColumns.filter(column => column !== sourceOrLabel))
            : setHiddenColumns([...hiddenColumns, sourceOrLabel]), onMove: handleMove })) : null;
};
exports.ColumnsSelectorItem = ColumnsSelectorItem;
const padRanks = (ranks, length) => ranks.concat(Array.from({ length: length - ranks.length }, (_, i) => ranks.length + i));
const fieldLabelMatchesFilter = (fieldLabel, columnFilter) => columnFilter
    ? diacritic
        .clean(fieldLabel)
        .toLowerCase()
        .includes(diacritic.clean(columnFilter).toLowerCase())
    : true;
//# sourceMappingURL=ColumnsSelectorItem.js.map