import * as React from 'react';
import { useDataTableRenderContext, genericMemo, } from 'ra-core';
import { DataTableCell } from "./DataTableCell.js";
import { DataTableHeadCell } from "./DataTableHeadCell.js";
import { ColumnsSelectorItem } from "./ColumnsSelectorItem.js";
const DataTableColumnImpl = React.forwardRef((props, ref) => {
    const renderContext = useDataTableRenderContext();
    switch (renderContext) {
        case 'columnsSelector':
            return React.createElement(ColumnsSelectorItem, { ...props });
        case 'header':
            return React.createElement(DataTableHeadCell, { ...props, ref: ref });
        case 'data':
            return React.createElement(DataTableCell, { ...props, ref: ref });
        case 'footer':
            return React.createElement(DataTableCell, { ...props, ref: ref });
    }
});
export const DataTableColumn = genericMemo(DataTableColumnImpl);
//# sourceMappingURL=DataTableColumn.js.map