import * as React from 'react';
import { RecordContextProvider, useDataTableDataContext } from 'ra-core';
import { TableBody, useThemeProps } from '@mui/material';
import { styled } from '@mui/material/styles';
import clsx from 'clsx';
import { DataTableClasses } from "./DataTableRoot.js";
import { DataTableRow } from "./DataTableRow.js";
import { useDataTableRowSxContext } from "./DataTableRowSxContext.js";
const PREFIX = 'RaDataTableBody';
export const DataTableBody = React.memo(React.forwardRef((inProps, ref) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { children, row: TableRow = DataTableRow, className, ...rest } = props;
    const data = useDataTableDataContext();
    const rowSx = useDataTableRowSxContext();
    return (React.createElement(TableBodyStyled, { ref: ref, className: clsx('datatable-body', className, DataTableClasses.tbody), ...rest }, data?.map((record, rowIndex) => (React.createElement(RecordContextProvider, { value: record, key: record.id ?? `row${rowIndex}` },
        React.createElement(TableRow, { className: clsx(DataTableClasses.row, {
                [DataTableClasses.rowEven]: rowIndex % 2 === 0,
                [DataTableClasses.rowOdd]: rowIndex % 2 !== 0,
            }), sx: rowSx?.(record, rowIndex) }, children))))));
}));
DataTableBody.displayName = 'DataTableBody';
const TableBodyStyled = styled(TableBody, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})(() => ({}));
//# sourceMappingURL=DataTableBody.js.map