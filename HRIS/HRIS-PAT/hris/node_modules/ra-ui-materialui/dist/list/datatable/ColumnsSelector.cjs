"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ColumnsSelector = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const react_dom_1 = require("react-dom");
const ra_core_1 = require("ra-core");
const material_1 = require("@mui/material");
const Search_1 = __importDefault(require("@mui/icons-material/Search"));
const button_1 = require("../../button/index.cjs");
const ResettableTextField_1 = require("../../input/ResettableTextField.cjs");
/**
 * Render DataTable.Col elements in the ColumnsButton selector using a React Portal.
 *
 * @see ColumnsButton
 */
const ColumnsSelector = ({ children }) => {
    const translate = (0, ra_core_1.useTranslate)();
    const { storeKey, defaultHiddenColumns } = (0, ra_core_1.useDataTableStoreContext)();
    const [columnRanks, setColumnRanks] = (0, ra_core_1.useStore)(`${storeKey}_columnRanks`);
    const [_hiddenColumns, setHiddenColumns] = (0, ra_core_1.useStore)(storeKey, defaultHiddenColumns);
    const elementId = `${storeKey}-columnsSelector`;
    const [container, setContainer] = React.useState(() => typeof document !== 'undefined'
        ? document.getElementById(elementId)
        : null);
    // on first mount, we don't have the container yet, so we wait for it
    React.useEffect(() => {
        if (container &&
            typeof document !== 'undefined' &&
            document.body.contains(container))
            return;
        // look for the container in the DOM every 100ms
        const interval = setInterval(() => {
            const target = document.getElementById(elementId);
            if (target)
                setContainer(target);
        }, 100);
        // stop looking after 500ms
        const timeout = setTimeout(() => clearInterval(interval), 500);
        return () => {
            clearInterval(interval);
            clearTimeout(timeout);
        };
    }, [elementId, container]);
    const [columnFilter, setColumnFilter] = React.useState('');
    if (!container)
        return null;
    const childrenArray = react_1.Children.toArray(children);
    const paddedColumnRanks = padRanks(columnRanks ?? [], childrenArray.length);
    const shouldDisplaySearchInput = childrenArray.length > 5;
    return (0, react_dom_1.createPortal)(React.createElement(material_1.Box, { sx: { pt: 1 } },
        shouldDisplaySearchInput ? (React.createElement(material_1.Box, { sx: { mb: 1 } },
            React.createElement(ResettableTextField_1.ResettableTextField, { hiddenLabel: true, label: "", value: columnFilter, onChange: e => {
                    if (typeof e === 'string') {
                        setColumnFilter(e);
                        return;
                    }
                    setColumnFilter(e.target.value);
                }, placeholder: translate('ra.action.search_columns', {
                    _: 'Search columns',
                }), InputProps: {
                    endAdornment: (React.createElement(material_1.InputAdornment, { position: "end" },
                        React.createElement(Search_1.default, { color: "disabled" }))),
                }, resettable: true, autoFocus: true, size: "small" }))) : null,
        React.createElement(material_1.MenuList, { sx: { pt: 0 } }, paddedColumnRanks.map((position, index) => (React.createElement(ra_core_1.DataTableColumnRankContext.Provider, { value: position, key: index },
            React.createElement(ra_core_1.DataTableColumnFilterContext.Provider, { value: columnFilter, key: index }, childrenArray[position]))))),
        React.createElement(material_1.Box, { className: "columns-selector-actions", sx: { textAlign: 'center', mb: 1 } },
            React.createElement(button_1.Button, { onClick: () => {
                    setColumnRanks(undefined);
                    setHiddenColumns(defaultHiddenColumns);
                }, label: translate('ra.action.reset', {
                    _: 'Reset',
                }) }))), container);
};
exports.ColumnsSelector = ColumnsSelector;
const padRanks = (ranks, length) => ranks.concat(Array.from({ length: length - ranks.length }, (_, i) => ranks.length + i));
//# sourceMappingURL=ColumnsSelector.js.map