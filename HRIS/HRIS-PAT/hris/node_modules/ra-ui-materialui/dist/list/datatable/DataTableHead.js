import * as React from 'react';
import { memo } from 'react';
import { TableCell, TableHead, TableRow } from '@mui/material';
import { styled, useThemeProps, } from '@mui/material/styles';
import clsx from 'clsx';
import { useDataTableConfigContext, useDataTableDataContext, useDataTableCallbacksContext, } from 'ra-core';
import ExpandAllButton from "../datagrid/ExpandAllButton.js";
import { SelectPageCheckbox } from "./SelectPageCheckbox.js";
import { DataTableClasses } from "./DataTableRoot.js";
/**
 * The default DataTable Head component.
 *
 * Renders select all checkbox as well as column head buttons used for sorting.
 */
export const DataTableHead = memo((inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { children, className, sx } = props;
    const { expand, expandSingle, hasBulkActions = false, } = useDataTableConfigContext();
    const data = useDataTableDataContext();
    const { handleToggleItem } = useDataTableCallbacksContext();
    const hasExpand = !!expand;
    return (React.createElement(TableHeadStyled, { className: clsx(className, DataTableClasses.thead), sx: sx },
        React.createElement(TableRow, { className: clsx(DataTableClasses.row, DataTableClasses.headerRow) },
            hasExpand && (React.createElement(TableCell, { padding: "none", variant: "head", className: clsx(DataTableClasses.headerCell, DataTableClasses.expandHeader) }, !expandSingle && data ? (React.createElement(ExpandAllButton, { classes: DataTableClasses, ids: data.map(record => record.id) })) : null)),
            hasBulkActions && handleToggleItem && (React.createElement(TableCell, { padding: "checkbox", variant: "head", className: DataTableClasses.headerCell },
                React.createElement(SelectPageCheckbox, null))),
            children)));
});
DataTableHead.displayName = 'DataTableHead';
const PREFIX = 'RaDataTableHead';
const TableHeadStyled = styled(TableHead, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})(() => ({}));
//# sourceMappingURL=DataTableHead.js.map