import * as React from 'react';
import { memo } from 'react';
import SearchIcon from '@mui/icons-material/Search';
import { InputAdornment } from '@mui/material';
import { FilterLiveForm, useTranslate } from 'ra-core';
import { TextInput } from "../../input/index.js";
/**
 * Form and search input for doing a full-text search filter.
 *
 * Triggers a search on change (with debounce).
 *
 * @example
 *
 * const FilterPanel = () => (
 *     <Card>
 *         <CardContent>
 *             <FilterLiveSearch source="title" />
 *         </CardContent>
 *     </Card>
 * );
 */
export const FilterLiveSearch = memo((props) => {
    const translate = useTranslate();
    const { source = 'q', label = translate('ra.action.search'), placeholder, ...rest } = props;
    return (React.createElement(FilterLiveForm, null,
        React.createElement(TextInput, { resettable: true, helperText: false, source: source, InputProps: {
                endAdornment: (React.createElement(InputAdornment, { position: "end" },
                    React.createElement(SearchIcon, { color: "disabled" }))),
            }, size: "small", label: rest.hiddenLabel ? false : label, placeholder: placeholder ?? (rest.hiddenLabel ? label : undefined), ...rest })));
});
//# sourceMappingURL=FilterLiveSearch.js.map