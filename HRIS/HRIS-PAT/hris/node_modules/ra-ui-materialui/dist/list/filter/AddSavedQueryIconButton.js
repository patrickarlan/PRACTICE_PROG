import * as React from 'react';
import { useState } from 'react';
import { IconButton } from '@mui/material';
import AddIcon from '@mui/icons-material/AddCircleOutline';
import { useTranslate } from 'ra-core';
import { AddSavedQueryDialog } from "./AddSavedQueryDialog.js";
export const AddSavedQueryIconButton = (props) => {
    const [open, setOpen] = useState(false);
    const handleOpen = () => {
        setOpen(true);
    };
    const handleClose = () => {
        setOpen(false);
    };
    const translate = useTranslate();
    return (React.createElement(React.Fragment, null,
        React.createElement(IconButton, { "aria-label": translate('ra.saved_queries.new_label', {
                _: 'Save current query...',
            }), size: "small", onClick: handleOpen, ...props },
            React.createElement(AddIcon, null)),
        React.createElement(AddSavedQueryDialog, { open: open, onClose: handleClose })));
};
//# sourceMappingURL=AddSavedQueryIconButton.js.map