"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.SimpleListItem = void 0;
const React = __importStar(require("react"));
const material_1 = require("@mui/material");
const ra_core_1 = require("ra-core");
const SimpleListItem = (props) => {
    const { children, linkType, rowClick, rowIndex, rowSx, rowStyle } = props;
    const resource = (0, ra_core_1.useResourceContext)(props);
    const record = (0, ra_core_1.useRecordContext)(props);
    const navigate = (0, ra_core_1.useNavigate)();
    // If we don't have a function to get the path, we can compute the path immediately and set the href
    // on the Link correctly without onClick (better for accessibility)
    const isFunctionLink = typeof linkType === 'function' || typeof rowClick === 'function';
    const pathForRecord = (0, ra_core_1.useGetPathForRecord)({
        link: isFunctionLink ? false : linkType ?? rowClick,
        resource,
    });
    const getPathForRecord = (0, ra_core_1.useGetPathForRecordCallback)();
    const handleClick = (0, ra_core_1.useEvent)(async () => {
        // No need to handle non function linkType or rowClick
        if (!isFunctionLink)
            return;
        if (!record)
            return;
        const link = typeof linkType === 'function'
            ? linkType(record, record.id)
            : typeof rowClick === 'function'
                ? (record, resource) => rowClick(record.id, resource, record)
                : false;
        const path = await getPathForRecord({
            record,
            resource,
            link,
        });
        if (path === false || path == null) {
            return;
        }
        navigate(path);
    });
    if (!record)
        return null;
    if (isFunctionLink) {
        return (React.createElement(material_1.ListItem, { disablePadding: true, sx: {
                '.MuiListItem-container': {
                    width: '100%',
                },
            } },
            React.createElement(material_1.ListItemButton, { onClick: handleClick, style: rowStyle ? rowStyle(record, rowIndex) : undefined, sx: rowSx?.(record, rowIndex) }, children)));
    }
    if (pathForRecord) {
        return (React.createElement(material_1.ListItem, { disablePadding: true, sx: {
                '.MuiListItem-container': {
                    width: '100%',
                },
            } },
            React.createElement(material_1.ListItemButton, { component: ra_core_1.LinkBase, to: pathForRecord, style: rowStyle ? rowStyle(record, rowIndex) : undefined, sx: rowSx?.(record, rowIndex) }, children)));
    }
    return (React.createElement(material_1.ListItem, { sx: {
            '.MuiListItem-container': {
                width: '100%',
            },
        } }, children));
};
exports.SimpleListItem = SimpleListItem;
//# sourceMappingURL=SimpleListItem.js.map