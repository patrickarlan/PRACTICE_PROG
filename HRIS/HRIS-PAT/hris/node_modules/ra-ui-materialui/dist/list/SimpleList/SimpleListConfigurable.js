import * as React from 'react';
import { useResourceContext, usePreference } from 'ra-core';
import { Configurable } from "../../preferences/index.js";
import { SimpleList } from "./SimpleList.js";
import { SimpleListEditor } from "./SimpleListEditor.js";
export const SimpleListConfigurable = ({ preferenceKey, ...props }) => {
    const resource = useResourceContext(props);
    return (React.createElement(Configurable, { editor: React.createElement(SimpleListEditor, null), preferenceKey: preferenceKey || `${resource}.SimpleList`, sx: { display: 'block' } },
        React.createElement(SimpleListWithPreferences, { ...props })));
};
const SimpleListWithPreferences = (props) => {
    const [primaryTextFromStore] = usePreference('primaryText');
    const [secondaryTextFromStore] = usePreference('secondaryText');
    const [tertiaryTextFromStore] = usePreference('tertiaryText');
    return (React.createElement(SimpleList, { ...props, primaryText: primaryTextFromStore || props.primaryText, secondaryText: secondaryTextFromStore || props.secondaryText, tertiaryText: tertiaryTextFromStore || props.tertiaryText }));
};
//# sourceMappingURL=SimpleListConfigurable.js.map