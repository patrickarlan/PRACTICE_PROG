"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Count = void 0;
const React = __importStar(require("react"));
const ra_core_1 = require("ra-core");
const material_1 = require("@mui/material");
const Error_1 = __importDefault(require("@mui/icons-material/Error"));
const Link_1 = require("../Link.cjs");
/**
 * Fetch and render the number of records of a given resource
 *
 * Relies on dataProvider.getList() returning a total property
 *
 * @example // Display the number of records in the current resource (based on ResourceContext)
 * <Count />
 *
 * @example // Display the number of posts
 * <Count resource="posts" />
 *
 * @example // Display the number of published posts
 * <Count resource="posts" filter={{ is_published: true }}/>
 *
 * @example // Display the number of posts, with a custom Typography variant
 * <Count resource="posts" variant="h1" />
 *
 * @see ReferenceManyCount for a similar component which fetches the number of records related to the current one
 */
const Count = (props) => {
    const { filter, sort, link, resource: resourceFromProps, timeout = 1000, ...rest } = props;
    const resource = (0, ra_core_1.useResourceContext)(props);
    if (!resource) {
        throw new Error('The Count component must be used inside a ResourceContext or must be passed a resource prop.');
    }
    const oneSecondHasPassed = (0, ra_core_1.useTimeout)(timeout);
    const createPath = (0, ra_core_1.useCreatePath)();
    const { total, isPending, error } = (0, ra_core_1.useGetList)(resource, {
        filter,
        sort,
        pagination: { perPage: 1, page: 1 },
    });
    const body = isPending ? (oneSecondHasPassed ? (React.createElement(material_1.CircularProgress, { size: 14 })) : ('')) : error ? (React.createElement(Error_1.default, { color: "error", fontSize: "small", titleAccess: "error" })) : (total);
    return link ? (
    // @ts-ignore TypeScript complains that the props for <a> aren't the same as for <span>
    React.createElement(Link_1.Link, { to: {
            pathname: createPath({ resource, type: 'list' }),
            search: filter ? `filter=${JSON.stringify(filter)}` : undefined,
        }, variant: "body2", onClick: e => e.stopPropagation(), ...rest }, body)) : (React.createElement(material_1.Typography, { component: "span", variant: "body2", ...rest }, body));
};
exports.Count = Count;
//# sourceMappingURL=Count.js.map