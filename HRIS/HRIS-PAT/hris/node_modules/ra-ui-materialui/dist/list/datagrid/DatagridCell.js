import * as React from 'react';
import TableCell from '@mui/material/TableCell';
import clsx from 'clsx';
const DatagridCell = React.forwardRef(({ className, field, record, resource, ...rest }, ref) => (React.createElement(TableCell, { className: clsx(className, field.props.cellClassName), align: field.props.textAlign || field.type.textAlign, ref: ref, ...rest }, field)));
// What? TypeScript loses the displayName if we don't set it explicitly
DatagridCell.displayName = 'DatagridCell';
export default DatagridCell;
//# sourceMappingURL=DatagridCell.js.map