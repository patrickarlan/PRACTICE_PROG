import * as React from 'react';
import { Children, isValidElement, useCallback } from 'react';
import { useListContextWithProps, useTranslate, } from 'ra-core';
import { Checkbox, TableCell, TableHead, TableRow } from '@mui/material';
import clsx from 'clsx';
import DatagridHeaderCell from "./DatagridHeaderCell.js";
import { DatagridClasses } from "./useDatagridStyles.js";
import ExpandAllButton from "./ExpandAllButton.js";
import { useDatagridContext } from "./useDatagridContext.js";
/**
 * The default Datagrid Header component.
 *
 * Renders select all checkbox as well as column header buttons used for sorting.
 */
export const DatagridHeader = (props) => {
    const { children, className, hasExpand = false, hasBulkActions = false, isRowSelectable, } = props;
    const translate = useTranslate();
    const { sort, data, onSelect, selectedIds, setSort } = useListContextWithProps(props);
    const { expandSingle } = useDatagridContext();
    const updateSortCallback = useCallback(event => {
        event.stopPropagation();
        if (!setSort)
            return;
        const newField = event.currentTarget.dataset.field;
        const newOrder = sort?.field === newField
            ? sort?.order === 'ASC'
                ? 'DESC'
                : 'ASC'
            : event.currentTarget.dataset.order;
        setSort({ field: newField, order: newOrder });
    }, [sort?.field, sort?.order, setSort]);
    const updateSort = setSort ? updateSortCallback : null;
    const handleSelectAll = useCallback(event => {
        if (!onSelect || !selectedIds || !data)
            return;
        onSelect(event.target.checked
            ? selectedIds.concat(data
                .filter(record => !selectedIds.includes(record.id) &&
                (!isRowSelectable ||
                    isRowSelectable(record)))
                .map(record => record.id))
            : // We should only unselect the ids present in the current page
                selectedIds.filter(id => !data.some(record => record.id === id)));
    }, [data, onSelect, isRowSelectable, selectedIds]);
    const selectableIds = Array.isArray(data)
        ? isRowSelectable
            ? data
                .filter(record => isRowSelectable(record))
                .map(record => record.id)
            : data.map(record => record.id)
        : [];
    return (React.createElement(TableHead, { className: clsx(className, DatagridClasses.thead) },
        React.createElement(TableRow, { className: clsx(DatagridClasses.row, DatagridClasses.headerRow) },
            hasExpand && (React.createElement(TableCell, { padding: "none", className: clsx(DatagridClasses.headerCell, DatagridClasses.expandHeader) }, !expandSingle && data ? (React.createElement(ExpandAllButton, { ids: data.map(record => record.id) })) : null)),
            hasBulkActions && selectedIds && (React.createElement(TableCell, { padding: "checkbox", className: DatagridClasses.headerCell },
                React.createElement(Checkbox, { inputProps: {
                        'aria-label': translate('ra.action.select_all', { _: 'Select all' }),
                    }, className: "select-all", color: "primary", checked: selectedIds.length > 0 &&
                        selectableIds.length > 0 &&
                        selectableIds.every(id => selectedIds.includes(id)), onChange: handleSelectAll, onClick: e => e.stopPropagation() }))),
            Children.map(children, (field, index) => isValidElement(field) ? (React.createElement(DatagridHeaderCell, { className: clsx(DatagridClasses.headerCell, `column-${field.props.source}`), sort: sort, field: field, isSorting: sort?.field ===
                    (field.props.sortBy ||
                        field.props.source), key: field.props.source || index, updateSort: updateSort || undefined })) : null))));
};
DatagridHeader.displayName = 'DatagridHeader';
//# sourceMappingURL=DatagridHeader.js.map