import * as React from 'react';
import { memo } from 'react';
import IconButton from '@mui/material/IconButton';
import ExpandMoreIcon from '@mui/icons-material/ExpandMore';
import { useTranslate } from 'ra-core';
const ExpandRowButton = ({ expanded, expandContentId, ...props }) => {
    const translate = useTranslate();
    return (React.createElement(IconButton, { "aria-label": translate(expanded ? 'ra.action.close' : 'ra.action.expand'), "aria-expanded": expanded, "aria-controls": expandContentId, tabIndex: -1, "aria-hidden": "true", component: "div", ...props, size: "small" },
        React.createElement(ExpandMoreIcon, { fontSize: "inherit" })));
};
export default memo(ExpandRowButton);
//# sourceMappingURL=ExpandRowButton.js.map