"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.DatagridHeader = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const ra_core_1 = require("ra-core");
const material_1 = require("@mui/material");
const clsx_1 = __importDefault(require("clsx"));
const DatagridHeaderCell_1 = __importDefault(require("./DatagridHeaderCell.cjs"));
const useDatagridStyles_1 = require("./useDatagridStyles.cjs");
const ExpandAllButton_1 = __importDefault(require("./ExpandAllButton.cjs"));
const useDatagridContext_1 = require("./useDatagridContext.cjs");
/**
 * The default Datagrid Header component.
 *
 * Renders select all checkbox as well as column header buttons used for sorting.
 */
const DatagridHeader = (props) => {
    const { children, className, hasExpand = false, hasBulkActions = false, isRowSelectable, } = props;
    const translate = (0, ra_core_1.useTranslate)();
    const { sort, data, onSelect, selectedIds, setSort } = (0, ra_core_1.useListContextWithProps)(props);
    const { expandSingle } = (0, useDatagridContext_1.useDatagridContext)();
    const updateSortCallback = (0, react_1.useCallback)(event => {
        event.stopPropagation();
        if (!setSort)
            return;
        const newField = event.currentTarget.dataset.field;
        const newOrder = sort?.field === newField
            ? sort?.order === 'ASC'
                ? 'DESC'
                : 'ASC'
            : event.currentTarget.dataset.order;
        setSort({ field: newField, order: newOrder });
    }, [sort?.field, sort?.order, setSort]);
    const updateSort = setSort ? updateSortCallback : null;
    const handleSelectAll = (0, react_1.useCallback)(event => {
        if (!onSelect || !selectedIds || !data)
            return;
        onSelect(event.target.checked
            ? selectedIds.concat(data
                .filter(record => !selectedIds.includes(record.id) &&
                (!isRowSelectable ||
                    isRowSelectable(record)))
                .map(record => record.id))
            : // We should only unselect the ids present in the current page
                selectedIds.filter(id => !data.some(record => record.id === id)));
    }, [data, onSelect, isRowSelectable, selectedIds]);
    const selectableIds = Array.isArray(data)
        ? isRowSelectable
            ? data
                .filter(record => isRowSelectable(record))
                .map(record => record.id)
            : data.map(record => record.id)
        : [];
    return (React.createElement(material_1.TableHead, { className: (0, clsx_1.default)(className, useDatagridStyles_1.DatagridClasses.thead) },
        React.createElement(material_1.TableRow, { className: (0, clsx_1.default)(useDatagridStyles_1.DatagridClasses.row, useDatagridStyles_1.DatagridClasses.headerRow) },
            hasExpand && (React.createElement(material_1.TableCell, { padding: "none", className: (0, clsx_1.default)(useDatagridStyles_1.DatagridClasses.headerCell, useDatagridStyles_1.DatagridClasses.expandHeader) }, !expandSingle && data ? (React.createElement(ExpandAllButton_1.default, { ids: data.map(record => record.id) })) : null)),
            hasBulkActions && selectedIds && (React.createElement(material_1.TableCell, { padding: "checkbox", className: useDatagridStyles_1.DatagridClasses.headerCell },
                React.createElement(material_1.Checkbox, { inputProps: {
                        'aria-label': translate('ra.action.select_all', { _: 'Select all' }),
                    }, className: "select-all", color: "primary", checked: selectedIds.length > 0 &&
                        selectableIds.length > 0 &&
                        selectableIds.every(id => selectedIds.includes(id)), onChange: handleSelectAll, onClick: e => e.stopPropagation() }))),
            react_1.Children.map(children, (field, index) => (0, react_1.isValidElement)(field) ? (React.createElement(DatagridHeaderCell_1.default, { className: (0, clsx_1.default)(useDatagridStyles_1.DatagridClasses.headerCell, `column-${field.props.source}`), sort: sort, field: field, isSorting: sort?.field ===
                    (field.props.sortBy ||
                        field.props.source), key: field.props.source || index, updateSort: updateSort || undefined })) : null))));
};
exports.DatagridHeader = DatagridHeader;
exports.DatagridHeader.displayName = 'DatagridHeader';
//# sourceMappingURL=DatagridHeader.js.map