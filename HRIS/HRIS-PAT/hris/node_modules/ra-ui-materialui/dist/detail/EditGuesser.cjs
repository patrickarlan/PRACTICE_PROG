"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.EditGuesserView = exports.EditGuesser = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const ra_core_1 = require("ra-core");
const inflection_1 = require("inflection");
const EditView_1 = require("./EditView.cjs");
const editFieldTypes_1 = require("./editFieldTypes.cjs");
const EditGuesser = (props) => {
    const { resource, id, mutationMode, mutationOptions, queryOptions, redirect, transform, disableAuthentication, ...rest } = props;
    return (React.createElement(ra_core_1.EditBase, { resource: resource, id: id, mutationMode: mutationMode, mutationOptions: mutationOptions, queryOptions: queryOptions, redirect: redirect, transform: transform, disableAuthentication: disableAuthentication },
        React.createElement(exports.EditGuesserView, { ...rest })));
};
exports.EditGuesser = EditGuesser;
const EditGuesserView = (props) => {
    const resource = (0, ra_core_1.useResourceContext)(props);
    if (!resource) {
        throw new Error(`<EditGuesser> was called outside of a ResourceContext and without a resource prop. You must set the resource prop.`);
    }
    const { record } = (0, ra_core_1.useEditContext)();
    const [child, setChild] = (0, react_1.useState)(null);
    const { enableLog = process.env.NODE_ENV === 'development', ...rest } = props;
    (0, react_1.useEffect)(() => {
        setChild(null);
    }, [resource]);
    (0, react_1.useEffect)(() => {
        if (record && !child) {
            const inferredElements = (0, ra_core_1.getElementsFromRecords)([record], editFieldTypes_1.editFieldTypes);
            const inferredChild = new ra_core_1.InferredElement(editFieldTypes_1.editFieldTypes.form, null, inferredElements);
            setChild(inferredChild.getElement());
            if (!enableLog)
                return;
            const representation = inferredChild.getRepresentation();
            const components = ['Edit']
                .concat(Array.from(new Set(Array.from(representation.matchAll(/<([^/\s>]+)/g))
                .map(match => match[1])
                .filter(component => component !== 'span'))))
                .sort();
            console.log(`Guessed Edit:

import { ${components.join(', ')} } from 'react-admin';

export const ${(0, inflection_1.capitalize)((0, inflection_1.singularize)(resource))}Edit = () => (
    <Edit>
${representation}
    </Edit>
);`);
        }
    }, [record, child, resource, enableLog]);
    return React.createElement(EditView_1.EditView, { ...rest }, child);
};
exports.EditGuesserView = EditGuesserView;
//# sourceMappingURL=EditGuesser.js.map