import * as React from 'react';
import { useEffect, useState } from 'react';
import { ShowBase, InferredElement, getElementsFromRecords, useResourceContext, useShowContext, } from 'ra-core';
import { capitalize, singularize } from 'inflection';
import { ShowView } from "./ShowView.js";
import { showFieldTypes } from "./showFieldTypes.js";
export const ShowGuesser = ({ id, queryOptions, resource, ...rest }) => (React.createElement(ShowBase, { id: id, resource: resource, queryOptions: queryOptions },
    React.createElement(ShowGuesserView, { ...rest })));
export const ShowGuesserView = (props) => {
    const resource = useResourceContext(props);
    if (!resource) {
        throw new Error(`<ShowGuesser> was called outside of a ResourceContext and without a resource prop. You must set the resource prop.`);
    }
    const { record } = useShowContext();
    const [child, setChild] = useState(null);
    const { enableLog = process.env.NODE_ENV === 'development', ...rest } = props;
    useEffect(() => {
        setChild(null);
    }, [resource]);
    useEffect(() => {
        if (record && !child) {
            const inferredElements = getElementsFromRecords([record], showFieldTypes);
            const inferredChild = new InferredElement(showFieldTypes.show, null, inferredElements);
            setChild(inferredChild.getElement());
            if (!enableLog)
                return;
            const representation = inferredChild.getRepresentation();
            const components = ['Show']
                .concat(Array.from(new Set(Array.from(representation.matchAll(/<([^/\s>]+)/g))
                .map(match => match[1])
                .filter(component => component !== 'span'))))
                .filter(component => !component.match(/[A-Za-z]+\.[A-Za-z]+/i))
                .sort();
            console.log(`Guessed Show:

import { ${components.join(', ')} } from 'react-admin';

export const ${capitalize(singularize(resource))}Show = () => (
    <Show>
${inferredChild.getRepresentation()}
    </Show>
);`);
        }
    }, [record, child, resource, enableLog]);
    return React.createElement(ShowView, { ...rest }, child);
};
//# sourceMappingURL=ShowGuesser.js.map