import * as React from 'react';
import { useResourceDefinition } from 'ra-core';
import TopToolbar from "../layout/TopToolbar.js";
import { ListButton } from "../button/index.js";
/**
 * Action Toolbar for the Create view
 *
 * Internal component. If you want to add or remove actions for a Create view,
 * write your own CreateActions Component. Then, in the <Create> component,
 * use it in the `actions` prop to pass a custom component.
 *
 * @example
 *     import Button from '@mui/material/Button';
 *     import { TopToolbar, Create, ListButton } from 'react-admin';
 *
 *     const PostCreateActions = () => (
 *         <TopToolbar>
 *             <ListButton />
 *             // Add your custom actions here //
 *             <Button color="primary" onClick={customAction}>Custom Action</Button>
 *         </TopToolbar>
 *     );
 *
 *     export const PostCreate = (props) => (
 *         <Create actions={<PostCreateActions />} {...props}>
 *             ...
 *         </Create>
 *     );
 */
export const CreateActions = (props) => {
    const { hasList } = useResourceDefinition(props);
    return (React.createElement(TopToolbar, { ...sanitizeRestProps(props) }, hasList && React.createElement(ListButton, null)));
};
const sanitizeRestProps = ({ hasCreate, hasEdit, hasList, hasShow, resource, ...rest }) => rest;
//# sourceMappingURL=CreateActions.js.map