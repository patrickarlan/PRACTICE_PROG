"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTabbedFormTabFullPath = exports.TabbedFormTabs = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const Tabs_1 = __importDefault(require("@mui/material/Tabs"));
const ra_core_1 = require("ra-core");
const TabbedFormTabs = (props) => {
    const { children, url, syncWithLocation, value, ...rest } = props;
    const params = (0, ra_core_1.useParams)();
    // params will include eventual parameters from the root pathname and * for the remaining part
    // which should match the tabs paths
    const tabValue = params['*'];
    return (React.createElement(Tabs_1.default, { value: syncWithLocation ? tabValue : value, indicatorColor: "primary", ...rest }, react_1.Children.map(children, (tab, index) => {
        if (!(0, react_1.isValidElement)(tab))
            return null;
        // Builds the full tab which is the concatenation of the last matched route in the
        // TabbedShowLayout hierarchy (ex: '/posts/create', '/posts/12', , '/posts/12/show')
        // and the tab path.
        // This will be used as the Tab's value
        const tabPath = (0, exports.getTabbedFormTabFullPath)(tab, index);
        return (0, react_1.cloneElement)(tab, {
            intent: 'header',
            value: syncWithLocation ? tabPath : index,
            syncWithLocation,
        });
    })));
};
exports.TabbedFormTabs = TabbedFormTabs;
const getTabbedFormTabFullPath = (tab, index) => tab.props.path != null ? tab.props.path : index > 0 ? index.toString() : '';
exports.getTabbedFormTabFullPath = getTabbedFormTabFullPath;
//# sourceMappingURL=TabbedFormTabs.js.map