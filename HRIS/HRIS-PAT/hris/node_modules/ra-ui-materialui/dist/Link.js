import * as React from 'react';
import clsx from 'clsx';
import { styled, Link as MuiLink, useThemeProps, } from '@mui/material';
import { LinkBase } from 'ra-core';
export const Link = (inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { to, children, className, ...rest } = props;
    return (React.createElement(StyledMuiLink, { component: LinkBase, to: to, className: clsx(LinkClasses.link, className), ...rest }, children));
};
const PREFIX = 'RaLink';
export const LinkClasses = {
    link: `${PREFIX}-link`,
};
const StyledMuiLink = styled(MuiLink, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({}); // @see https://mui.com/material-ui/guides/typescript/#complications-with-the-component-prop
//# sourceMappingURL=Link.js.map