import React from 'react';
import DashboardIcon from '@mui/icons-material/Dashboard';
import { useBasename } from 'ra-core';
import { MenuItemLink } from "./MenuItemLink.js";
export const DashboardMenuItem = (props) => {
    const basename = useBasename();
    const { leftIcon = React.createElement(DashboardIcon, null), to = `${basename}/`, primaryText = 'ra.page.dashboard', ...rest } = props;
    return (React.createElement(MenuItemLink, { leftIcon: leftIcon, to: to, primaryText: primaryText, ...rest }));
};
//# sourceMappingURL=DashboardMenuItem.js.map