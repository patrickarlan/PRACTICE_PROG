import * as React from 'react';
import { Alert, Button } from '@mui/material';
import { useTranslate } from 'ra-core';
export const ApplicationUpdatedNotification = React.forwardRef((props, ref) => {
    const { ButtonProps, updateText = 'ra.action.update_application', notificationText = 'ra.notification.application_update_available', ...alertProps } = props;
    const translate = useTranslate();
    const handleButtonClick = () => {
        window.location.reload();
    };
    return (React.createElement(Alert, { ref: ref, severity: "info", action: React.createElement(Button, { color: "inherit", size: "small", onClick: handleButtonClick, ...ButtonProps }, translate(updateText, { _: updateText })), ...alertProps }, translate(notificationText, { _: notificationText })));
});
//# sourceMappingURL=ApplicationUpdatedNotification.js.map