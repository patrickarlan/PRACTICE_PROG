"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.Title = void 0;
const React = __importStar(require("react"));
const react_1 = require("react");
const react_dom_1 = require("react-dom");
const ra_core_1 = require("ra-core");
const PageTitle_1 = require("./PageTitle.cjs");
const PageTitleConfigurable_1 = require("./PageTitleConfigurable.cjs");
const Title = (props) => {
    const { defaultTitle, title, preferenceKey, ...rest } = props;
    const [container, setContainer] = (0, react_1.useState)(() => typeof document !== 'undefined'
        ? document.getElementById('react-admin-title')
        : null);
    // on first mount, we don't have the container yet, so we wait for it
    (0, react_1.useEffect)(() => {
        setContainer(container => {
            const isInTheDom = typeof document !== 'undefined' &&
                document.body.contains(container);
            if (container && isInTheDom)
                return container;
            return typeof document !== 'undefined'
                ? document.getElementById('react-admin-title')
                : null;
        });
    }, []);
    if (!container)
        return null;
    (0, ra_core_1.warning)(!defaultTitle && !title, 'Missing title prop in <Title> element');
    const pageTitle = preferenceKey === false ? (React.createElement(PageTitle_1.PageTitle, { title: title, defaultTitle: defaultTitle, ...rest })) : (React.createElement(PageTitleConfigurable_1.PageTitleConfigurable, { title: title, defaultTitle: defaultTitle, preferenceKey: preferenceKey, ...rest }));
    return React.createElement(React.Fragment, null, (0, react_dom_1.createPortal)(pageTitle, container));
};
exports.Title = Title;
//# sourceMappingURL=Title.js.map