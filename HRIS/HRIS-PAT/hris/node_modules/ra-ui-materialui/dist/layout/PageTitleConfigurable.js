import * as React from 'react';
import { usePreferenceInput, usePreference, useRecordContext, useTranslate, useLocation, } from 'ra-core';
import { TextField } from '@mui/material';
import { Configurable } from "../preferences/index.js";
import { PageTitle } from "./PageTitle.js";
export const PageTitleEditor = () => {
    const field = usePreferenceInput('', '');
    return (React.createElement("form", null,
        React.createElement(TextField, { label: "title", variant: "filled", size: "small", fullWidth: true, sx: { mb: 1 }, ...field })));
};
export const PageTitleConfigurable = ({ preferenceKey, title, defaultTitle, ...props }) => {
    const { pathname } = useLocation();
    return (React.createElement(Configurable, { editor: React.createElement(PageTitleEditor, null), preferenceKey: preferenceKey || `${pathname}.title`, sx: {
            '&.RaConfigurable-editMode': {
                margin: '2px',
            },
        } },
        React.createElement(PageTitleConfigurableInner, { title: title, defaultTitle: defaultTitle, ...props })));
};
const PageTitleConfigurableInner = ({ title, defaultTitle, ...props }) => {
    const [titleFromPreferences] = usePreference();
    const translate = useTranslate();
    const record = useRecordContext();
    return typeof titleFromPreferences === 'string' ? (React.createElement("span", { className: props.className, ...props }, translate(titleFromPreferences, {
        ...record,
        _: titleFromPreferences,
    }))) : (React.createElement(React.Fragment, null,
        React.createElement(PageTitle, { title: title, defaultTitle: defaultTitle, ...props })));
};
//# sourceMappingURL=PageTitleConfigurable.js.map