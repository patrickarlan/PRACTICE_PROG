import * as React from 'react';
import { useTranslate } from 'ra-core';
export const PageTitle = ({ title, defaultTitle, className, ...rest }) => {
    const translate = useTranslate();
    return (React.createElement("span", { className: className }, !title ? (React.createElement("span", { ...rest }, defaultTitle)) : typeof title === 'string' ? (React.createElement("span", { ...rest }, translate(title, { _: title }))) : (title)));
};
//# sourceMappingURL=PageTitle.js.map