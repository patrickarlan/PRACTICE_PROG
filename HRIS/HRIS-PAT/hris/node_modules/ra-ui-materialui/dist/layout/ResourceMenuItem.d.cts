import * as React from 'react';
import { MenuItemLinkProps } from "./MenuItemLink.cjs";
export declare const ResourceMenuItem: ({ name, ...rest }: ResourceMenuItemProps) => React.JSX.Element | null;
export interface ResourceMenuItemProps extends Omit<MenuItemLinkProps, 'to'>, Partial<Pick<MenuItemLinkProps, 'to'>> {
    name: string;
}
//# sourceMappingURL=ResourceMenuItem.d.ts.map