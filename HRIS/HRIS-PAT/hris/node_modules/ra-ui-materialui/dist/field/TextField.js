import * as React from 'react';
import Typography from '@mui/material/Typography';
import { styled, useThemeProps, } from '@mui/material/styles';
import { useFieldValue, useTranslate, genericMemo } from 'ra-core';
import { sanitizeFieldRestProps } from "./sanitizeFieldRestProps.js";
const TextFieldImpl = (inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { className, emptyText, ...rest } = props;
    const translate = useTranslate();
    const value = useFieldValue(props);
    return (React.createElement(StyledTypography, { component: "span", variant: "body2", className: className, ...sanitizeFieldRestProps(rest) }, value != null && typeof value !== 'string'
        ? value.toString()
        : value ||
            (emptyText ? translate(emptyText, { _: emptyText }) : null)));
};
// what? TypeScript loses the displayName if we don't set it explicitly
TextFieldImpl.displayName = 'TextFieldImpl';
export const TextField = genericMemo(TextFieldImpl);
const PREFIX = 'RaTextField';
const StyledTypography = styled(Typography, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=TextField.js.map