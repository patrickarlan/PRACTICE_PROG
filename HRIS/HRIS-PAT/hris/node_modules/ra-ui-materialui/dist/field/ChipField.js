import * as React from 'react';
import { styled, useThemeProps, } from '@mui/material/styles';
import { Chip, Typography } from '@mui/material';
import clsx from 'clsx';
import { useFieldValue, useTranslate, genericMemo } from 'ra-core';
import { sanitizeFieldRestProps } from "./sanitizeFieldRestProps.js";
const ChipFieldImpl = (inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { className, emptyText, ...rest } = props;
    const value = useFieldValue(props);
    const translate = useTranslate();
    if (value == null || value === '') {
        if (!emptyText || emptyText.length === 0) {
            return null;
        }
        return (React.createElement(Typography, { component: "span", variant: "body2", className: className, ...sanitizeFieldRestProps(rest) }, emptyText && translate(emptyText, { _: emptyText })));
    }
    return (React.createElement(StyledChip, { className: clsx(ChipFieldClasses.chip, className), label: value, ...sanitizeFieldRestProps(rest) }));
};
ChipFieldImpl.displayName = 'ChipFieldImpl';
export const ChipField = genericMemo(ChipFieldImpl);
const PREFIX = 'RaChipField';
const ChipFieldClasses = {
    chip: `${PREFIX}-chip`,
};
const StyledChip = styled(Chip, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({
    [`&.${ChipFieldClasses.chip}`]: { cursor: 'inherit' },
});
//# sourceMappingURL=ChipField.js.map