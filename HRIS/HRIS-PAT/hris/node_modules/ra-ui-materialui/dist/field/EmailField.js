import * as React from 'react';
import Typography from '@mui/material/Typography';
import { Link } from '@mui/material';
import { styled, useThemeProps, } from '@mui/material/styles';
import { useFieldValue, useTranslate, genericMemo } from 'ra-core';
import { sanitizeFieldRestProps } from "./sanitizeFieldRestProps.js";
const EmailFieldImpl = (inProps) => {
    const props = useThemeProps({
        props: inProps,
        name: PREFIX,
    });
    const { className, emptyText, ...rest } = props;
    const value = useFieldValue(props);
    const translate = useTranslate();
    if (value == null) {
        return emptyText ? (React.createElement(Typography, { component: "span", variant: "body2", className: className, ...sanitizeFieldRestProps(rest) }, emptyText && translate(emptyText, { _: emptyText }))) : null;
    }
    return (React.createElement(StyledLink, { className: className, href: `mailto:${value}`, onClick: stopPropagation, variant: "body2", ...sanitizeFieldRestProps(rest) }, value));
};
EmailFieldImpl.displayName = 'EmailFieldImpl';
export const EmailField = genericMemo(EmailFieldImpl);
// useful to prevent click bubbling in a Datagrid with rowClick
const stopPropagation = e => e.stopPropagation();
const PREFIX = 'RaEmailField';
const StyledLink = styled(Link, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=EmailField.js.map