import React from 'react';
import Tab from '@mui/material/Tab';
import { useTranslate } from 'ra-core';
import { capitalize } from 'inflection';
/**
 * Single tab that selects a locale in a TranslatableFields component.
 * @see TranslatableFields
 */
export const TranslatableFieldsTab = (props) => {
    const { locale, groupKey = '', ...rest } = props;
    const translate = useTranslate();
    return (React.createElement(Tab, { id: `translatable-header-${groupKey}${locale}`, label: translate(`ra.locales.${groupKey}${locale}`, {
            _: capitalize(locale),
        }), ...rest }));
};
//# sourceMappingURL=TranslatableFieldsTab.js.map