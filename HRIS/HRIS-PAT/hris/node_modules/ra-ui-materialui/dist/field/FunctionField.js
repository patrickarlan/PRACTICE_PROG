import * as React from 'react';
import { useMemo } from 'react';
import { useRecordContext } from 'ra-core';
import Typography from '@mui/material/Typography';
import { sanitizeFieldRestProps } from "./sanitizeFieldRestProps.js";
/**
 * Field using a render function
 *
 * @example
 * <FunctionField
 *     source="last_name" // used for sorting
 *     label="Name"
 *     render={record => `${record.first_name} ${record.last_name}`}
 * />
 */
export const FunctionField = (props) => {
    const { className, source = '', render, ...rest } = props;
    const record = useRecordContext(props);
    return useMemo(() => record ? (React.createElement(Typography, { component: "span", variant: "body2", className: className, ...sanitizeFieldRestProps(rest) }, render(record, source))) : null, [className, record, source, render, rest]);
};
//# sourceMappingURL=FunctionField.js.map