"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReferenceManyField = void 0;
const react_1 = __importDefault(require("react"));
const ra_core_1 = require("ra-core");
const material_1 = require("@mui/material");
const Offline_1 = require("../Offline.cjs");
/**
 * Render related records to the current one.
 *
 * You must define the fields to be passed to the iterator component as children.
 *
 * @example Display all the comments of the current post as a datagrid
 * <ReferenceManyField reference="comments" target="post_id">
 *     <Datagrid>
 *         <TextField source="id" />
 *         <TextField source="body" />
 *         <DateField source="created_at" />
 *         <EditButton />
 *     </Datagrid>
 * </ReferenceManyField>
 *
 * @example Display all the books by the current author, only the title
 * <ReferenceManyField reference="books" target="author_id">
 *     <SingleFieldList>
 *         <ChipField source="title" />
 *     </SingleFieldList>
 * </ReferenceManyField>
 *
 * By default, restricts the displayed values to 25. You can extend this limit
 * by setting the `perPage` prop.
 *
 * @example
 * <ReferenceManyField perPage={10} reference="comments" target="post_id">
 *    ...
 * </ReferenceManyField>
 *
 * By default, orders the possible values by id desc. You can change this order
 * by setting the `sort` prop (an object with `field` and `order` properties).
 *
 * @example
 * <ReferenceManyField sort={{ field: 'created_at', order: 'DESC' }} reference="comments" target="post_id">
 *    ...
 * </ReferenceManyField>
 *
 * Also, you can filter the query used to populate the possible values. Use the
 * `filter` prop for that.
 *
 * @example
 * <ReferenceManyField filter={{ is_published: true }} reference="comments" target="post_id">
 *    ...
 * </ReferenceManyField>
 */
const ReferenceManyField = (props) => {
    const translate = (0, ra_core_1.useTranslate)();
    const { children, pagination, empty, offline = defaultOffline, render, ...controllerProps } = props;
    return (react_1.default.createElement(ra_core_1.ReferenceManyFieldBase, { ...controllerProps, empty: typeof empty === 'string' ? (react_1.default.createElement(material_1.Typography, { component: "span", variant: "body2" }, translate(empty, { _: empty }))) : (empty), render: props => {
            const { isPaused, isPending, isPlaceholderData } = props;
            const shouldRenderOffline = isPaused &&
                (isPending || isPlaceholderData) &&
                offline !== undefined &&
                offline !== false;
            return (react_1.default.createElement(react_1.default.Fragment, null,
                shouldRenderOffline
                    ? offline
                    : render
                        ? render(props)
                        : children,
                pagination));
        } }));
};
exports.ReferenceManyField = ReferenceManyField;
const defaultOffline = react_1.default.createElement(Offline_1.Offline, { variant: "inline" });
//# sourceMappingURL=ReferenceManyField.js.map