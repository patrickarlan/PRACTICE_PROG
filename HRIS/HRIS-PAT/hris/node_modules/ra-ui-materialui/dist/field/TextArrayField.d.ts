import * as React from 'react';
import { ChipProps, StackProps } from '@mui/material';
import { FieldProps } from "./types.js";
/**
 * Renders an array of scalar values using MUI Stack and Chips.
 *
 * @example
 * // const post = {
 * //   id: 123
 * //   genres: [
 * //     'Fiction',
 * //     'Historical Fiction',
 * //     'Classic Literature',
 * //     'Russian Literature',
 * //   ]
 * // };
 * const PostShow = () => (
 *    <Show>
 *       <SimpleShowLayout>
 *           <TextArrayField source="genres" />
 *      </SimpleShowLayout>
 *   </Show>
 * );
 */
export declare const TextArrayField: <RecordType extends Record<string, any> = Record<string, any>>(props: TextArrayFieldProps<RecordType>) => React.JSX.Element;
export interface TextArrayFieldProps<RecordType extends Record<string, any> = Record<string, any>> extends FieldProps<RecordType>, Omit<StackProps, 'textAlign' | 'color'>, Pick<ChipProps, 'size' | 'color' | 'variant'> {
}
//# sourceMappingURL=TextArrayField.d.ts.map