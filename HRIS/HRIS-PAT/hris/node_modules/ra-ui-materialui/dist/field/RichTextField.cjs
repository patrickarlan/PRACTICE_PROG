"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeTags = exports.RichTextField = void 0;
const React = __importStar(require("react"));
const Typography_1 = __importDefault(require("@mui/material/Typography"));
const ra_core_1 = require("ra-core");
const dompurify_1 = __importDefault(require("dompurify"));
const sanitizeFieldRestProps_1 = require("./sanitizeFieldRestProps.cjs");
const styles_1 = require("@mui/material/styles");
/**
 * Render an HTML string as rich text
 *
 * Note: This component leverages the `dangerouslySetInnerHTML` attribute,
 * but uses the DomPurify library to sanitize the HTML before rendering it.
 *
 * It means it is safe from Cross-Site Scripting (XSS) attacks - but it's still
 * a good practice to sanitize the value server-side.
 *
 * @example
 * <RichTextField source="description" />
 *
 * @example // remove all tags and output text only
 * <RichTextField source="description" stripTags />
 */
const RichTextFieldImpl = (inProps) => {
    const props = (0, styles_1.useThemeProps)({
        props: inProps,
        name: PREFIX,
    });
    const { className, emptyText, stripTags = false, purifyOptions, ...rest } = props;
    const value = (0, ra_core_1.useFieldValue)(props);
    const translate = (0, ra_core_1.useTranslate)();
    return (React.createElement(StyledTypography, { className: className, variant: "body2", component: "span", ...(0, sanitizeFieldRestProps_1.sanitizeFieldRestProps)(rest) }, (value == null || value === '') && emptyText ? (translate(emptyText, { _: emptyText })) : stripTags ? ((0, exports.removeTags)(value)) : (React.createElement("span", { dangerouslySetInnerHTML: {
            __html: dompurify_1.default.sanitize(value, purifyOptions || {}),
        } }))));
};
RichTextFieldImpl.displayName = 'RichTextFieldImpl';
exports.RichTextField = (0, ra_core_1.genericMemo)(RichTextFieldImpl);
const removeTags = (input) => input ? input.replace(/<[^>]+>/gm, '') : '';
exports.removeTags = removeTags;
const PREFIX = 'RaRichTextField';
const StyledTypography = (0, styles_1.styled)(Typography_1.default, {
    name: PREFIX,
    overridesResolver: (props, styles) => styles.root,
})({});
//# sourceMappingURL=RichTextField.js.map