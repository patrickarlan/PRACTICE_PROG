## 🧪 Test Checklist

- [x] Created `Navbar.tsx` using semantic HTML (`<header>`, `<nav>`) and sticky styling
- [x] Created `Footer.tsx` using semantic HTML and dynamic year rendering
- [x] Created `ProjectCard.tsx` with a TypeScript props interface
- [x] Updated `App.tsx` to render the Navbar, Footer, a Hero section, and 3 `<ProjectCard />` instances
- [x] Styled all components using Tailwind CSS classes

---

## 📊 Final Score: 10/10

**Summary:**
- [x] Activity completed (`App.tsx` and custom components reviewed)
- [x] All checklist items pass without console or compiler warnings

**Ready for:** Lesson 5.2 — React State & Hooks!
