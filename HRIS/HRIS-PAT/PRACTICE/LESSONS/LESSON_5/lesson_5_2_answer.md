## 🧪 Test Checklist

- [x] Implemented a "Like" counter using `useState` in the hero section of `App.tsx`
- [x] Used `useEffect` to log a console message when the "Likes" state variable changes
- [x] Added `isExpanded` state inside `ProjectCard.tsx` to conditionally render extra detail text
- [x] Used `useEffect` with an empty array `[]` to console log a welcome message on app startup
- [x] Created the custom hook `src/hooks/useProjects.ts` to manage and return the list of projects
- [x] Updated `App.tsx` to use the `useProjects` custom hook, render cards dynamically, and allow adding a project dynamically

---

## 📊 Final Score: 10/10

**Summary:**
- [x] Activity completed (`App.tsx`, `ProjectCard.tsx`, and `useProjects.ts` reviewed)
- [x] All checklist items pass without compilation warnings

**Ready for:** Lesson 5.3 — Fetching Data from an API!
