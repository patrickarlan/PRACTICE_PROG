## 🧪 Test Checklist

- [x] Created `src/hooks/useGitHubProfile.ts` with a working fetch hook
- [x] Created `src/components/GitHubCard.tsx` that uses the hook and handles all 3 states (loading, error, success)
- [x] Added `<GitHubCard />` to `App.tsx` inside an `id="about"` section
- [x] The app renders live GitHub profile data (avatar, name, bio, repo count, followers) from the API
- [x] The `response.ok` check is present in the hook

---

## 📊 Final Score: 10/10

**Summary:**
- [x] Activity completed (`useGitHubProfile.ts`, `GitHubCard.tsx`, and `App.tsx` reviewed)
- [x] All checklist items pass without compilation warnings

**Ready for:** Lesson 5.4 — React Router & Multi-Page Navigation!
